<!-- 出处：docs/Phase-0-MVP.md L1300–L1315｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-8 · 课程更新对话框（文本档） ✅ **用户验收通过 2026-09-13（Steven 原话"验收通过，效果非常好"）**

- **做什么**：Steven #2 —— 像 AI 对话框一样，**用户把任何有关课程的更新扔进去**（文字 / 粘贴网页内容），AI 解析后更新 task structure。
- **定位（ADR-016 / ADR-017）**：**对话框是兜底，不是入口**；它是"电子秘书"的入口，不是又一个日历。每加一个对话用例都要先问「这能不能自动化」——长期目标是让它被自动化（邮件 / 官方接口）取代。
- **实现记录（2026-09-13，commit `45d3bc3` 已 push）**：
  - ✅ `components/course-update-fab.tsx`：全局浮窗 FAB，挂 `AppShell` → **所有已登录页可见**；课程下拉 + 文本输入 + 解析预览 + 确认。
  - ✅ `POST /api/v1/tasks`：批量建 `source='manual'`，强制 `status=pending / is_derived=false / submission_state=null`，校验课程归属。
  - ✅ `DELETE /api/v1/tasks/:id`：仅 `source='manual'` 软删；同步来源拒绝。
  - ✅ `POST /api/v1/tasks/parse`：LLM 解析为结构化预览（`runStructured`，`purpose='course_update_parse'` v1，温度 0），**不落库**；exam 变更进 `warnings`。
  - ✅ `lib/tasks/manual.ts`（纯函数校验 + 日期归一）+ `scripts/regress-manual-tasks.ts`（口径回归）。
  - ⏳ **用户验收**：运行时登录态端到端烟测（解析→确认→落库）待 Steven 浏览器验证。
- **硬约束**：
  - 🔴 **AI 解析 → 用户确认 → 才落库**。对话输入没有 `sourceExcerpt` 那样的可验证锚点，幻觉会直接进日程。
  - 🔴 **不篡改权威数据**：exam 派生行改 `exam_dates`，用户已确认项（`is_confirmed`）不动。
  - **零输入成本**：用户什么都不用说也能识别（Steven 要的是"随手扔进去"）。

