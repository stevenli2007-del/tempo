<!-- 出处：docs/Phase-0-MVP.md L1316–L1328｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-8b · 对话框「检索 / 更新」档（Follow-up） ✅ **用户验收通过 2026-09-13（Steven 原话"验收通过，效果非常好"）**

- **做什么（Steven 原话）**：「用户输入内容之后，我们先检索一遍；如果当前内容中没有匹配的，就直接新增；如果有匹配的（甚至检索到多个可能匹配的），都列举出来，让用户选择改哪个。」
- **实现记录（2026-09-13）**：
  - ✅ `lib/tasks/match.ts`（纯函数）：缩写归一（`HW==Homework`）+ 编辑距离 + 二元组 Dice；`matchTasks()` 打分 → 过滤 → 排序 → 截断。**确定性、零 IO、可单测**（回归 39/39）。
  - ✅ `GET /api/v1/tasks/search?courseId=&q=&limit=`：该课候选检索。**不写 `llm_runs`、不花 token**。
  - ✅ `PATCH /api/v1/tasks/:id` 扩展：内容字段（`title`/`dueDate`）**仅 `source='manual'` 可改**；派生任务 422 `derived_task_immutable`、非 manual 422 `source_not_editable`（引导去源头，不静默忽略）。
  - ✅ `components/course-update-fab.tsx` 升级：解析后对每个参考**先检索** → 0 命中显示「将新建」；有命中的**列举候选让用户选**（默认选中首个可改的手动匹配）+ 永远带「都不是，新建」出口；非 manual 候选置灰并写明原因。
  - ✅ `types/task.ts` 新增 `TaskCandidate` / `TaskMatch`。
- **🔴 架构判断**：**匹配用确定性代码，不用 LLM**（LLM 只负责"从文本抽标题"）。理由：LLM 会幻觉出不存在的任务 id，而改错用户的日程代价与幻觉进日程同级。
- **决策（写进 PATCH 准入）**：`source='canvas'` 的内容真相在 Canvas，在这里改会被下次同步覆盖 → 拦下并引导去 Canvas 或等同步；考试归 `exam_dates`（ADR-004）。**手动任务才是 Tempo 里可自由编辑的那一类。**
- **自测**：`next build` 全过（类型 + 编译）；纯函数回归 **48/48**（含 follow-up 年份推断）。运行时端到端烟测**已由 Steven 浏览器验收通过 2026-09-13**。

