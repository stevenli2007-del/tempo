<!-- 出处：docs/Phase-0-MVP.md L1722–L1763｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-23 · practice test 生成（类型 A） ✅ **Steven 验收通过 2026-09-18**

- **做什么（类型 A = 忠实自测，2026-09-17 拍板）**：用户在资料区**主动选**某个 past exam + 它的 answer key → 抓内容 → **隐藏答案做成自测卷** → 可选「讲解这道题的解法」→ 结果进 3-18 消息栏。
- **依赖**：P0-3-18 ✅、P0-3-19 ✅、P0-3-27 ✅。
- **验收标准**：① 选 past exam + key → 生成自测卷（答案隐藏）；② 「讲解」给出该题解法；③ **不出现 LLM 编造的新题**。
- **✅ 验收结论（2026-09-18，Steven 原话"验收没有问题了"）**：三条验收标准全部命中 —— ① 资料区试卷行「自测卷」→ 生成卷子、答案默认收起（`<details>`）；② 点「讲解这道题的解法」出解法；③ 无 LLM 编造的新题（题源只有那份试卷）。验收时 Steven 未报出任何问题。**附带一处待 Steven 拍板的小遗留**：`material`（资料索引）同样是**空写入**却仍叫「确认」，自测卷已改「知道了」—— 属 P0-3-19 范围的文案统一，未随手改动（`regress-messages.ts` 有条断言钉着），账面记于 `lib/messages/view.ts` 的 `ACK_LABEL` 注释。
- **🔴 边界**：**类型 B（LLM 出新题）已否**（物理/化学题会幻觉错公式）；slides 解析最贵（视觉模型），本卡只处理 past exam 的 PDF/文本。

**交付（`e00710d` 代码 + `8060656` 迁移复核，2026-09-18，均已 push + Vercel success）**

- **入口**：资料区「像试卷」的行长出「自测卷」按钮（`isExamLike()` 启发式），跳
  `/courses/[id]/practice-tests/new?exam=<fileId>`。⚠️ **是页面路由、不是 API 端点**
  （渲染即真实下载两份文件 + 一次模型调用），所以链接**必须** `prefetch={false}`。
- **配对**（`lib/practice-test/pairing.ts`，零依赖纯函数）：先算**剥掉 KEY 词后的指纹**、
  要求**指纹相等**才算候选，再按（同目录 > 同后缀 > 名字短）排序。
  **相似度判据是错的** —— 它会把 `PracticeMidterm1KEY_F23` 配给 `PracticeMidterm2_F23`（同门课、
  名字高度相似、年份相同，看起来完全合理却彻底错误）；**指纹为空即放弃配对**。
  实库两形态：Chem 1A 子目录 `Answer Keys` 年份后缀 × 5 对 / Math 53 兄弟目录同名文件。
- **切题**（`prompt.ts` 硬规则 9 条）：绝不自己出题 / 绝不自己算答案 / 题干照原文抄 /
  **答案文件不是题源**；出口 `validatePracticeOutput()` 再校验一次（**prompt 是"请求"，校验才是"保证"**）。
  题源**只有**试卷那一份文件。可手动换 key（`resolveKeyChoice`：用户指定 > 自动配对 > 没有）。
- **🔴 答案读不出来 → 整张卷子失败**（`status='failed'`），**不降级成"没有答案的卷子"**——
  那会让用户以为那道题本来就没有答案（假信息比缺失更糟）。
- **缓存两表**（见 `Database.md` §3.17 / §3.18）：一份试卷一张卷（`unique (exam_file_id)`）、
  **失败也落行**（"别再重试"）、差量判据是**两个** `modified_at`（老师只换答案 key 也必须重算 ——
  否则旧卷子的答案是过期的谎话）。**索引路径仍零下载**，原文不落库不落盘不进日志（**ADR-027**）。
- **消息栏**（3-18 是唯一出口）：生成完只投**一条** `practice_test` 消息（去重键 `payload.practiceTestId`）；
  确认是**刻意的空写入**（卷子那一刻已落库）→ 按钮文案「**知道了**」；
  真正的行动入口是 `payload.paperPath` 派生成的「**打开自测卷 →**」—— 过 `readInternalPath` 守卫
  （**站内路径也要挡**：`//evil.com` 是协议相对 URL，与 `/courses/…` 只差一个字符）。
- **pptx 在自测卷路径上明确拒绝**（`PAPER_EXTENSIONS = ['pdf','docx']`）—— 要逐题题干，
  从排版型 slide 里切出来的"题"不可信；3-19b 的「一键总结」仍可读 pptx（那里要的是描述性提要）。
- **门禁**：`tsc` ✓ / `eslint` 全仓 **0 error**（2 条历史 warning）/ `build` ✓（新路由在路由表里）/
  `regress:practice-tests` **145 通过 0 失败**（样例用真账号真实文件名）/ `regress:messages` **108 通过 0 失败**；
  上线三段式：push → Vercel `success` → 无凭证探针 **307 → `/login`**（对照路径 404）。
- **迁移 `20260924000000_practice_tests.sql`** ✅ **已由 Steven 手跑 + 逐条真接口复核**
  （零残留探针五查：两表在 / **29 列逐一点名** / 枚举 CHECK 真换值 + 对照组 + `locale='fr'` 反向断言 /
  **RLS 双向**（anon 0 行、真实用户会话 1 行，含 `explanations → practice_tests → courses` **两跳**）/
  唯一键 `23505`；末了三处零残留自检均为 0 行）。
  ⚠️ 复核探针第一版**自己踩坑**：在同一个 `anon` client 上先 `verifyOtp()` 再"匿名读"→ 报出
  「anon 读到 1 行 —— RLS 没在拦」，真相是**探针在冒充登录用户**（详情见 `CodingRules.md` §10.2）。

