<!-- 出处：docs/Phase-0-MVP.md L1524–L1552｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-18 · 消息栏（侧栏入口 + 全屏对话页） ✅ **验收通过 2026-09-17 晚**

- **做什么**：把「对话框」从一个右下角浮窗升级为**一级导航 + 全屏页**，并成为**所有系统提案的唯一出口**。
  1. **侧栏新增「消息栏」**：`components/shell/sidebar.tsx` 的 `NAV` 加一项（`href: "/messages"`，图标 `MessageSquare`/`Inbox`）。
  2. **全屏页 `/messages`**：现有 `components/course-update-fab.tsx` 的对话框内容扩成整页（课程选择 + 文本/截图 + 解析 + 确认），移动端友好。
  3. **系统消息同流**：Canvas 定期刷新扫到的差异（如新 syllabus ≠ 用户上传）由系统以「消息」形式发进这条流，带 **「确认覆盖 / 忽略」** 两级动作。
- **数据**：`messages` 表（`type` / `payload` jsonb / `status`: `pending`|`accepted`|`dismissed` / `created_at`）。
  - **🔴【Steven 手动】**：建表 SQL 走 **Supabase SQL Editor**（项目约定：迁移不进 `supabase/migrations/` 自动执行）。执行顺序：① Bud 在对话里给出建表 SQL + 判据；② Steven 打开 Supabase Dashboard → SQL Editor 粘贴执行；③ Bud 跑只读探针（`select count(*) from messages`）确认表存在 + RLS 策略就位，再写代码。**Bud 不代跑 SQL Editor。**
  - 表 SQL 草案（待 3-18 开工时由 Bud 给出，含 RLS：`using (auth.uid() = user_id)`）：`id uuid pk default gen_random_uuid()` / `user_id uuid not null references auth.users` / `type text not null`（`syllabus_drift`|`practice_test`|`routine`|`material`）/ `payload jsonb not null` / `status text not null default 'pending'` / `created_at timestamptz default now()`。
- **依赖**：P0-3-17。
- **验收标准**：① 侧栏「消息栏」可点进全屏对话；② 系统提案可确认/忽略；③ 忽略不写、确认才写（ADR-015）；④ 浮窗 FAB 保留（ADR-016：兜底非入口）。
- **🔴 边界**：这是**唯一**提案出口，`P0-3-20` / `P0-3-23` 不许各做一套 UI。

##### 📋 开工前侦察记（Bud 2026-09-17 只读取证，未动代码）

**现状事实**（已核实）：
- `components/shell/sidebar.tsx` 的 `NAV` **只有 3 项**（`/dashboard` 课程面板 · `/courses` 我的课程 · `/settings` 设置与隐私）→ 新项插在第 3 位。
- `components/course-update-fab.tsx` = **725 行**（对话框状态机 + 课程选择 + 文本/截图 + 解析 + 确认），是本卡最主要的**复用资产**。
- `/messages` 路由**不存在**；`messages` 表**不存在**；`MessageSquare` 图标全仓未使用。

**🔴 三项开工前待 Steven 拍板**：
1. **用户自己的更新要不要落库？** 表 `type` 草案四项（`syllabus_drift` / `practice_test` / `routine` / `material`）**全是系统提案类型，没有用户消息**。若不落库 → 刷新后只剩系统消息、用户自己的话消失（与现在浮窗一致）。**Bud 倾向：只有系统提案落库**，用户消息瞬时。
2. **侧栏要不要 pending 徽标？** 不加 → 提案无人看见（本卡白做）；加 → 变成"催你来点"的钩子，与 ADR-016 hands-off 有摩擦。**Bud 倾向：加，但仅 `pending > 0` 时出现、处理完即消失。**
3. **浮窗与整页共用一份逻辑，还是各写一份？** 725 行浮窗若复制一份到 `/messages`，「确认才写」的判定就存在两处 —— **这正是 P0-3-15「分组判定 vs 行内渲染分叉」的同型 bug，且同样会 `tsc` / `eslint` / `build` 全绿**。**Bud 倾向：抽出共享的对话框状态机 + 写库调用，浮窗与整页都挂它。**

**⚠️ 验收通路（本卡自带的一个坑）**：验收标准②「系统提案可确认/忽略」**需要至少一条真提案**，但产生提案的 `P0-3-20` / `P0-3-23` **还在后面**。故本卡须自带一条**一次性种子提案**通路（一次性 SQL 插一行 `pending` → 在 UI 上真点「确认 / 忽略」→ 验完即删）。方案开工时交 Steven 确认。

**📌 迁移落地方式**：沿用 `P0-3-17` 的模式 —— Bud 写 `supabase/migrations/<ts>_messages.sql` 作为 SoT，但**由 Steven 手动粘贴到 SQL Editor 执行**（不自动跑）。

