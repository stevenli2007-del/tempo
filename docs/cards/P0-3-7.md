<!-- 出处：docs/Phase-0-MVP.md L1257–L1280｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-7 · 总览页可视化：周历 / 时间线 + 考试并入 + 进度条 ✅ **已验收 2026-09-13（commit `6c8b3b2`）**

- **实现记录（2026-09-13）**：
  - ✅ **开工前拍板三个决定**（Ask 阶段提案 + `AskUserQuestion`）：① 负担条**只算 `source='canvas'`**（分母宁可小不能脏）；② 周历**滚动 7 天、今天在最左**；③ 「最近要做的事」清单**保留**在周历下方。
  - ✅ **口径层**（`3-7a`）：新增 `lib/tasks/progress.ts`（**纯函数、零 IO**）：`isEffectivelyDone()` / `classifyDebt()` / `summarizeDebt()` / `buildWeekCalendar()` / `buildUpcomingExams()`；新增 `lib/time.ts`（`SCHOOL_TIME_ZONE` 从 dashboard 搬来，周历也要按学校日历日分桶，两处必须同一个值）。
  - ✅ **只读探针实测**（先用数据定口径，不靠猜）：生产库 49 行任务 → Canvas 45 / syllabus 4；**Canvas 已过期任务 13 条全部已被判定完成**；`submission_state = NULL` 仅 2 条且都未到期（`not_graded`/`on_paper` 类）→ 据此把 `NULL` 归入**「待确认」**而非「未交」（ADR-013：Canvas 不知道 ≠ 用户没交）。
  - ✅ **取数**：`lib/tasks.loadDebtTasks()`（`.eq('source','canvas')` **写在 SQL 里**，防止后人重构时把考试算进欠账）/ `loadUpcomingExams()`。均**新增**，不动 `loadTasks`（它的 `range` 契约只设上界，加下界会牵动公开 API，且 `.gte` 会把 NULL 行整批吃掉 —— `CodingRules §10.2` 同类坑）。
  - ✅ **修正原方案一处**：口径文件叫「进度条」但**不能**是进度条 —— 分母会随老师发布作业**自身膨胀**（全学期百分比会倒退）、且没有工作量字段。改成**「已到期作业」负担卡**：`未交`（红）+ `待确认`（琥珀）画段位，**已交不画段位**只出文字（"做完的事不是新闻"，ADR-016 R5），且**不出现任何百分比**。
  - ✅ **发现并补上一个真问题**：滚 7 天窗口下**一场考试都看不到** —— 实测 4 场考试全在 7 天之外（最近的 Unit 1 Exam 还有 9 天）。追加**「最近的考试」条**（`task_type='exam'`，不受 7 天限制，默认 3 条，显示"还有 N 天"）。**执行卡的「考试日期并入主 Calendar」靠它才真正兑现。**
  - ✅ **组件**：`components/overview/week-calendar.tsx`（服务端纯展示，7 列 + 今天高亮 + 顶部逾期整条 + lime 实心考试块 + 底部「日期待定」；pill **不做勾选**，只跳课程 —— 改 `status` 的唯一入口仍是 `TaskList`）/ `components/overview/debt-bar.tsx`（整体分段条 + 每门课未交条形图，已加 `data-debt-*` 便于探针）。
  - ✅ **消重**：`task-list.tsx` 与 dashboard 各写一份的"是否算完成"判定，提为共享的 `isEffectivelyDone()` —— 周历与清单必须用**完全相同**的规则过滤。
  - ✅ **接线**：dashboard 四路并行取数（卡片近期 / 清单 / 债务条 / 考试池）；债务卡只在 `hasCanvasLink` 时渲染（没连 Canvas 它永远是空的）；**查询失败时藏起卡片并明说**，不显示成"没有欠账"（CodingRules 7）。清单标题改「待办清单」（**不叫"全部待办"** —— 它其实是 7 天窗口 + 逾期，叫"全部"是静默的错误标签）。
  - ✅ **文档**：`API-Contract.md §5.1`（新增，口径全文：债务条三桶表 + 周历口径表 + 考试条 + 时区 + 明确不做清单）。
  - ✅ **自测**：`scripts/regress-progress.ts`（**新增常驻回归**，`npm run regress:progress`，纯函数零依赖）→ **17/17 PASS**；`npx tsc --noEmit` 干净；`npx eslint .` 只剩 `theme-toggle.tsx` 一处**既有**报错（与本卡无关）；`npm run build` 通过。
  - ✅ **运行时烟测**（真登录态，非"构建通过"）：`next start` + 真实会话 cookie 打 `/dashboard` → **HTTP 200、无服务端错误**；实读 `data-debt-overdue="0" / submitted="13" / unconfirmed="0"`；日历实际排出 周一 9/14（Chem 1A Week 3 Discussion Quiz、R4A I.5）/ 周三 9/16（R4A I.6）/ 周四 9/17（Chem 1A Homework 7），今天 9/13 周日高亮且为空；「最近的考试」条显示 **Unit 1 Exam 还有 9 天 / Unit 2 还有 37 天 / Unit 3 还有 58 天**（Final Exam 按 limit 3 截掉，仍在课程卡可见）。
- **验收路径（都能点，2026-09-13）**：
  1. `/dashboard` 顶部出现**「已到期作业」**卡：条上只有红/琥珀两段（无已交段、无百分比），文字为「未交 0 · 已交 13 · 共 13 件」+「到期的都交了。」
  2. 其下**「接下来 7 天」**：7 列，**今天（周日）在最左且有 lime 边框**；周一/周三/周四各有 pill；点任一 pill → 跳该课程详情页。
  3. 同一张卡底部**「最近的考试」**：Unit 1 Exam「还有 9 天 9/22 周二」—— **注意它不在 7 天窗口内，这是刻意的**（否则实测一场考试都看不到）。
  4. 再往下**「待办清单」**：仍是原来的列表与「已完成 21 项」折叠盒；**勾选完成的行为完全没变**（日历不提供勾选）。
- **做什么**：Steven #3 + #4 —— 去文字化（syllabus/outline/policies 冗长没人看）；「最近要做的事」改成**进度条 + Calendar**；**考试日期并进 dashboard 主页 Calendar**。Stride 已有 `.timeline` 与 `.week-calendar`（7 列 × 810px，事件分 flexible/reserved/exam）可直接参照。
- **🔴 依赖 P0-3-10**：数据准之前不做可视化。
- **关键约束**：**先定义进度的分子分母**（tasks 只有 dueDate，没有"工作量"概念），例如 7 天内已完成 / 总数；口径写进 `API-Contract.md`。

