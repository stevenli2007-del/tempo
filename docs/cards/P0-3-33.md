<!-- 新增卡（2026-09-19，Steven 提「发布前必须补的六件事」之一；不在拆分基线 f625a5a 内） -->

#### 🎫 P0-3-33 · 视觉区分强化（考试 / 课程色 / 水印）✅

> ✅ **Steven 验收通过 2026-09-20**（原话「验收没有问题。进入0-3-34」）。
> Commit：`00d7c0a`（15 改 + 2 新建，+310/−71）已 push + Vercel success；**生产域名实测** `/login` 与 `/signup` 均 200 且命中水印、响应头 `age: 0`（证明是新服务，非 CDN 旧副本）。
> 门禁：`tsc --noEmit` ✓ / `eslint .` 0 error（仅 2 条既有 warning）/ `build` ✓ / **17 条回归全绿**（`regress:messages` 109/0，含两条新断言）。
> 产物 CSS（`-F`，因 `/` 为转义写法）：`bg-<key>`×5 / `bg-<key>-bg`×5 / `text-<key>`×5 / `border-l-<key>`×5 / `bg-lime/10` / `bg-lime/20` / `border-lime-dark/30` / `border-lime-dark/40` / `hover:bg-lime/20` / `sr-only` —— 全部命中。
> **零迁移、零新环境变量**（`UpcomingTask` / `UpcomingTaskView` 各加一个字段，只多 select 一列既有 `task_type`）。
> 验收时的两处**如实标注**：① 唯一观感变化 = 周历考试 pill 由**实心 lime** 改为**浅底描边**（仍比灰底普通 pill 明显，Steven 已接受；想改回一行即可）；② 消息栏课程色**本卡未动**（`lib/messages/view.ts` 的 `courseToneClass` 按**课程名**哈希）→ 同一门课在消息栏与课程卡**可能不同色**，将来要统一可单开小卡。

- **做什么（Steven 原话 + 截图三张）**：①「考试和寻常任务要有区分」—— 周历里高亮了很好，
  但**今日任务列表**里考试和作业几乎没差别（只有个「考」字前缀）。②「不同学科区分不够，
  单单一颗很小的颜色亮点不足以区分」。③「登录页应该加我的水印：Build by Youcheng Li / UC Berkeley」。
  顺手带上 3-23 遗留：`material`（资料索引）消息同样是空写入却仍叫「确认」，应统一为「知道了」（R3）。
- **依赖**：P0-3-23 ✅、**依赖 P0-3-29**（先保证考试行的语义正确，再谈怎么画它）。
- **必读**：[`../CodingRules.md`](../CodingRules.md) §10（Tailwind v4 CSS-first）+ **ADR-017**（纯显示、GCal 也能做的 → 不做）。
- **grep 范围**：`components/**`、`app/(routes)/**`、`app/globals.css`。

**关键约束**

1. 🔴 **先结构与语义，后皮肤**：考试的**判定**只用既有单一来源（`lib/tasks/progress.ts` 与 exam 派生任务的属性），
   本卡只加视觉表现，**绝不新增第二套「这是不是考试」的判断**（同类教训见 3-15：判定分叉会让 40/83 条画错）。
2. 🔴 **Tailwind v4：类名写了 ≠ 类被生成**。课程色是运行时值，
   **不能拼 `bg-${color}`** —— 要么在 `app/globals.css` 的 `@theme` 里静态声明，要么走内联 style。
   验收必须 grep 产物 CSS 证明类真的存在。
3. 考试视觉语言**全站一致**（周历 pill / 今日任务行 / 课程详情 / BOX）：统一用「边框 + 底色 + 图标」三件套，
   而不是再加一个字。
   **验收时 Steven 拍板**：「考」字**保留**（不做成唯一线索即可）→ 落地 = 三件套 + 图标 + 「考」＋ `sr-only` 的「考试」。
4. 水印只加登录页（真实路径是 `app/(routes)/login` —— 本卡写卡时写的 `app/(auth)/login` **已过时**），**不许全站铺**（admin 味，且与 hands-off 调性冲突）。
   **验收时 Steven 追加**：注册页 `app/(routes)/signup` **也要**（两页共用 `components/auth/build-signature.tsx`，其余页面一律没有）。
5. `material` 按钮文案改动**必须同步改 `regress-messages.ts` 里钉住它的那条断言**（3-23 故意没动的那条）。

**验收标准**

| # | 标准 |
|---|---|
| ① | 同一场考试在周历 pill / 今日任务行 / 课程详情三处是**同一套**视觉语言 |
| ② | 课程色在课程卡、任务来源标记、考试 pill 上一致（不再是只有圆点） |
| ③ | 产物 CSS 里 grep 得到本卡新增的类（证明 Tailwind 真生成了） |
| ④ | 登录页出现 "Build by Youcheng Li · UC Berkeley" 水印，其他页面没有 |
| ⑤ | `material` 消息按钮显示「知道了」，`regress:messages` 全绿 |
