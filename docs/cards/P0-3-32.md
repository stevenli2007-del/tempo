<!-- 新增卡（2026-09-19，Steven 提「发布前必须补的六件事」之一；不在拆分基线 f625a5a 内） -->

#### 🎫 P0-3-32 · 新手引导 + 侧栏反馈链接（占位符）✅

> ✅ **Steven 验收通过 2026-09-20**（原话「好没问题了，0-3-32 收尾」）。
> Commits：`33a9900`（卡片组 + 侧栏）+ `b216275`（③④ 动图）+ `39a533b`（② 动图 + 文案纠错）—— 三个均已 push + Vercel success，四条静态资源在生产站实测 200。
> 门禁：`tsc` ✓ / `eslint .` 0 error（未新增 warning）/ `build` ✓ / `regress:onboarding` **69/0** / 产物 CSS 里 9 个新类名全部命中 / SSR 探针 29/0（临时探针已删、测试号已删）。
> **零迁移、零新环境变量**（`FEEDBACK_URL` 占位常量除外）。

**遗留（不是缺陷，是待办）**
- 🔵 `FEEDBACK_URL` = `https://example.com/tempo-feedback` 仍是**占位符**（`lib/constants.ts` 一行）。Steven 给 Google Form 链接后改这一行，侧栏 + 教程卡收尾链接一起生效。
- 🔵 第 ② 张卡的动图弹层里有**真实姓名与头像**（Canvas 账号菜单自带，动态浮层 → "先跑脚本改 DOM 再录"够不着）。Steven 明确接受；将来要遮可用固定坐标马赛克（弹层整段静止），**不必重录**。
- 🟢 「看过」标记是 cookie → **换浏览器 / 清缓存会再看一次**。这是「纯前端、不新增表」这张卡的既定代价，卡面约束 ④ 允许的范围内。

- **做什么（ Steven 原话）**：①「新用户登录后应首先进入教程页，几张可左右翻的卡片，
  教怎么去 Canvas Settings 配 access token 并和 Tempo 连起来，**每一步附跳转链接**。」
  ②「最左侧目录栏最下面除了 Course OS v0.1，还应直接贴一个**反馈链接**，点开就能写反馈。」
- **依赖**：P0-1-10 ✅（Demo Workspace 冷启动，两者要共存不打架）。
- **不必读**：数据层文档（`Database.md` / `Sync-Strategy.md` / `API-Contract.md`）—— 本卡不碰数据层。
- **grep 范围**：`app/(routes)/(auth)/**`、`components/layout/**`、`components/app-sidebar*`。

**关键约束**

1. 🔴 **反馈链接先放占位符**：配一个**全站唯一的常量**（`FEEDBACK_URL`），Steven 之后给 Google Form 链接直接替换。
   2026-09-19 现状 = 占位符。
2. 教程卡里的跳转链接是**写死的常量**（bCourses 首页 / Settings → + New Access Token），
   不接用户输入 → 仍然只放行 http(s)（沿用 `sourceUrl` 守卫）；外链 `target="_blank"` + `rel="noopener noreferrer"`。
3. **不许挡住冷启动**：已有 Demo Workspace（「先看看效果」）是降低流失的关键，
   教程不能被当成唯一入口；提供「跳过」+ 侧栏/设置页的「重看教程」入口。
4. 「看过」的记录必须是**每用户**（localStorage / cookie 都行，但要能重看），绝不能用新注册即见的假标记。
5. 纯前端，不新增表、不迁移、不加环境变量（除占位常量）。

**验收标准**

| # | 标准 |
|---|---|
| ① | 全新账号首次登录 → 引导卡第一屏；可左右翻 / 点步进入下一张 |
| ② | 每张配 `canvas` 跳转的卡片，链接可点且新开标签页 |
| ③ | 「跳过」有效；二次登录不再自动出现；「重看教程」入口随手可及 |
| ④ | 侧栏底部 Course OS v0.1 下方出现反馈链接，点开是占位目标；换常量即全站生效 |
| ⑤ | Demo Workspace 入口仍在同一屏可见（没被引导挤掉） |

#### 🎬 扩展（2026-09-20，验收前追加）：卡片加演示动图

Steven 原话：「能不能加几个动图 —— 打开 settings → 进 access token 生成页 → 生成，循环播放在卡片上。」

| 卡 | 动图 | 说明 |
|---|---|---|
| ① 为什么接 | 无 | 纯文字 |
| ② 打开 bCourses | `public/onboarding/open-canvas.mp4`（77KB / 7.9s / 1200×784） | 裁掉浏览器窗口与右侧 To Do 栏。⚠️ 弹层里有**真实姓名与头像**（Canvas 账号菜单自带，动态浮层脚本够不着）→ **Steven 2026-09-20 明确接受**（本来就公开在个人站/GitHub） |
| ③ 生成 Access Token | `public/onboarding/new-token.mp4`（495KB / 17.5s / 1200×848） | 裁掉浏览器窗口与右栏（登录邮箱在那里） |
| ④ 粘回 Tempo | `public/onboarding/connect.mp4`（143KB / 20.7s / 1200×744） | 裁掉浏览器窗口与 Tempo 侧栏；原片取 13–34s |

**实现要点**

1. `OnboardingStep` 新增 `media: { src, poster, caption } | null` —— **`null` 就不渲染**
   （指向不存在的文件等于画一个假东西，与「守卫判 null 时宁可不画链接」同一条纪律）。
2. 有动图的卡宽屏走**两列**（文字左、动图右），目的是让卡片**高度基本不变**，
   下面的「✨ 先看看效果」仍留在首屏（约束 ③）。窄屏退回单列，且**不给动图限高裁切** ——
   那会裁掉画面中间的对话框，正好是最该看的。
3. `autoplay` 三件套缺一不可：`muted`（不静音浏览器直接拒播）+ `playsInline`（iOS 否则强制全屏）+ `loop`。
4. 「减弱动态效果」**用纯 CSS 双元素切换**（视频 `motion-reduce:hidden`、封面图 `motion-reduce:block`），
   **不读 `matchMedia`** —— 后者要 `setState` 于 effect（撞 `react-hooks/set-state-in-effect`）且 SSR 会 hydration 不匹配。
5. 回归新增 20 条断言，最值钱的是 **`fs.existsSync`**：文件名打错时页面不报错、构建不报错，
   只是卡片上多一个空白框；另含体积闸门（单条 ≤700KB、封面 ≤120KB、合计 ≤2MB）。

**素材怎么来的**（原始 `.mov` 不进仓库 —— 二进制一旦进 git 历史就永久占位）

本机没有 brew/ffmpeg，用 npm 装一个静态二进制到 `/tmp`（不 sudo、不碰仓库）：

```bash
npm i --prefix /tmp/ffmpeg-tool ffmpeg-static
FF=/tmp/ffmpeg-tool/node_modules/ffmpeg-static/ffmpeg

$FF -i "原始A.mov" -vf "crop=1780:1258:420:640,scale=1200:-2,fps=15" -an \
    -c:v libx264 -crf 30 -preset medium -pix_fmt yuv420p -movflags +faststart \
    public/onboarding/new-token.mp4
$FF -ss 13 -i "原始B.mov" -t 21 -vf "crop=2584:1604:440:294,scale=1200:-2,fps=15" -an \
    -c:v libx264 -crf 30 -preset medium -pix_fmt yuv420p -movflags +faststart \
    public/onboarding/connect.mp4
$FF -ss 14 -i "原始A.mov" -frames:v 1 -vf "crop=1780:1258:420:640,scale=1200:-2" \
    -c:v libwebp -quality 82 public/onboarding/new-token-poster.webp

# ② 打开 bCourses（2026-09-20 补上；去掉浏览器窗口与右侧 To Do 栏，高度 1240 为的是包住整个弹层）
$FF -ss 0.3 -i "原始C.mov" -vf "crop=1900:1240:0:160,scale=1200:-2,fps=15" -an \
    -c:v libx264 -crf 30 -preset medium -pix_fmt yuv420p -movflags +faststart \
    public/onboarding/open-canvas.mp4
$FF -ss 7.2 -i "原始C.mov" -frames:v 1 -vf "crop=1900:1240:0:160,scale=1200:-2" \
    -c:v libwebp -quality 82 public/onboarding/open-canvas-poster.webp
```

**②的三条留档**（都是同一天回改的）

1. **文案与画面必须互相印证**：这条片子演的是「点**左侧栏最上方**的头像 → 菜单里的 Settings」，
   而卡片原稿写的是「左侧菜单**底部**找到 Account」+「**右上角**头像同样能进 Settings」——
   两处都与实际界面不符（Account 就在 Cal 校徽正下方）。**已按画面改正文案**。
   教训：文案里每个方位词都是可验证断言，写完要拿录屏对一遍。
2. **我第一版把这条片子判错了**：接触表上看到弹层里有「Accessibility Settings」，
   就把它当成"无障碍设置弹窗"而弃用；放到原分辨率一看，主题正是这张卡要教的路径。
   → 判"能不能用"必须看原分辨率（见 `CodingRules.md` §10.1 第 22 条 ③）。
3. **隐私**：弹层里的真实姓名与头像是 Canvas 账号菜单自带的，**住在动态浮层里**
   （"先跑脚本改 DOM 再录"够不着 —— 脚本跑完浮层才渲染）。Steven 2026-09-20 明确接受。
   将来若要遮：弹层在整段里**静止** → 固定坐标的模糊块就够，不必重录。

**未闭合**：无（四张卡里三张有动图，第 ① 张「为什么接」保持纯文字）。
