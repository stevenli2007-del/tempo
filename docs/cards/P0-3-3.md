<!-- 出处：docs/Phase-0-MVP.md L1223–L1241｜P0-3 执行卡｜2026-09-18 拆分，逐字保留 -->

#### 🎫 P0-3-3 · 设计基线：移植 Stride 令牌 + 应用外壳 ✅ 已验收 2026-09-13

- **做什么**：把 Harris 的 Stride（`Harris-Mansilla/Tempo`，纯 HTML/CSS/JS 单文件应用）的**设计令牌 + 布局范式**移植进我们的 Next + Tailwind。
  🔴 **移植的是令牌与布局，不是代码库** —— 他是 vanilla JS，我们是 React。**绝不能把他那个仓库当依赖源**（不引 CDN / 包），他后期会关仓，一关我们就断。
- **主仓约定（Steven 2026-09-13）**：主仓 = `stevenli2007-del/tempo`；`Harris-Mansilla/Tempo` 仅作 UI 参考、**后期关闭**。
- **设计令牌（直接从 `styles.css` 抄）**：
  - `--lime:#d8ed9d` / `--limeDark:#435727`
  - `--bg:#f7f8f4` · `--surface:#fff` · `--surface2:#f2f4ef` · `--line:#e4e8df`
  - `--ink:#182637` · `--muted:#758078`
  - `--nav:#f0f3eb` · `--navActive:#e1eacb` · Hero `--hero:#142238`
  - 课程色五组 purple / coral / blue / green / amber（各含亮色 + 浅背景）
  - 圆角：卡片 `18px` / 按钮 `10px` / 图标 `12px` / badge `6px`
  - 字体：系统字体栈，正文 `15px/1.5`，h1 `35px` / `-1.4px` / `650`，eyebrow `11px` 大写 `1.8px`
  - 骨架：固定侧栏 `220px` + 吸顶顶栏 `76px` + 卡片网格；断点 1550 / 1190 / 960 / 720 / 550（550 以下切底部移动导航）
  - 深色主题：`body[data-theme=dark]` 全套覆盖
- **Stride 的数据模型值得直接对齐**（省一轮设计）：五态任务机 `todo / progress / ready（已完成待提交）/ submitted / done`；`availability: open / waiting / **unknown（需核查）**`；`confidence: verified / user / review`；进度条两种（子步骤比例 / 课程完成数÷总数）；**无 streak**。
  > 这三个概念正好是我们 #7 三态、ADR-015「待确认」、R3「诚实标注不确定」、P0-3-7 进度条口径的现成答案。
- **关键约束**：先只做**外壳与令牌**，不动业务逻辑；深色主题下文字必须可读（系统提示的 IDE 主题规则同样适用）。

