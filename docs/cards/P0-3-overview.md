<!-- 出处：docs/Phase-0-MVP.md L1136–L1155 / L1173–L1176｜P0-3 排序纪律 + 明确移出 M3 + 执行卡前言｜2026-09-18 拆分，逐字保留 -->

**排序纪律（不是编号顺序）**：`3-3 设计基线 → 3-4 / 3-5 / 3-6 结构与内容 → 3-10 地基（提交状态 + 时区）→ 3-7 可视化 → 3-8 / 3-9 / 3-11 对话框与通道 → 3-14 主动提醒 → 3-15 完成态统一 → **3-16 Dashboard 精修 → 3-17 课程详情重排 → 3-18 消息栏 → 3-24 考试可编排+写入器 → 3-25 公告自动进站 → 3-26 回执+撤销 → 3-27 URL 抓取 → 3-19 资料索引 → 3-20 大纲漂移 → 3-23 practice test** → 3-12 扫尾 → 3-13 freeze`。
> ⚠️ **3-14 编号在 3-13 之后，但执行顺序在 3-12 之前** —— 沿用 3-7b 的「编号 ≠ 执行顺序」惯例（编号只服务可读性，顺序以本行为准）。
先定结构与内容、再上皮肤 —— 反过来做会把新界面**画两遍**。
🔴 **P0-3-10 是地基**：完成状态与日期都错着的时候做进度条（3-7）和激励，等于庆祝一件没发生的事（ADR-016）。**3-7 因此排在 3-10 之后，不在「结构与内容」段内**（Steven 2026-09-13 确认，已同步 `Roadmap.md` §4 顺序纪律）。

**明确移出 M3（避免含糊）**

| Steven 的 refine 条目 | 去向 | 理由 |
|---|---|---|
| 对话框提示语 / 删「已到期作业」/ 今日任务切片 | **M3 · P0-3-16** | 纯文案 + 信息架构，freeze 前闭合 |
| 课程详情一屏看全 / 需手动确认徽标 / 跳 Canvas / 饼图 / 每作业分数条 | **M3 · P0-3-17** | 纯前端 + 加 1 字段(加列)，freeze 前闭合 |
| 消息栏 + M3.5 主动进站期（对话框可编排 / 公告自动进站 / 回执撤销 / URL 抓取）/ 资料索引 / 大纲漂移检测 / practice test（类型 A） | **M3 · P0-3-18 / 3-24 / 3-25 / 3-26 / 3-27 / 3-19 / 3-20 / 3-23** | 2026-09-17 Steven 拍板**上移进 M3**（freeze 前做）；M3.5 系 O-13 三项新需求（公告/回执撤销/URL 抓取）落地，Steven 当晚拍板四项（§14 改 / OH 去掉 / 撤销 24h / 轮询每日 2 次）；架构与逐卡设计见 `docs/Phase-1-Design.md` + `Decisions.md` ADR-021 |
| ~~【验收衍生需求】外平台（Gradescope）成绩手报~~ | **已评估，暂不做**（2026-09-17 晚 Steven 决定） | 起因：Math 53 Discussion 的 9/4 quiz 实考且 Gradescope 出分 9.5/10，Canvas 无记录 → Tempo 显「已逾期」。**侦察结论（留档，别再查一遍）**：① 「已逾期」**不是 bug** —— `canBeOverdue()` 刻意保留 syllabus 考试/手动任务的 `null` 态可逾期（"说日子过了是陈述用户自己的记录"）；且 `PATCH /tasks/:id` 的 `status` 任何来源可改，**勾选框可点，勾上即解**。② 真缺口是「登记分数」，且 `submission_score` / `points_possible` **在 `canvas-tasks.ts` 的同步写入封闭集里** → 直接写入会被下次同步静默抹回 null，必须新增**用户自报层**（新列/新表 + 徽标注明来源，绝不冒充 Canvas）。③ **红线**：`submission_state` 永不由用户写。④ 依赖：截图识别需 `DASHSCOPE_API_KEY`（至今未配，3-9 未做真实识别验收）。 |
| ✅ **【P0-3-25 口径已定 = C】公告进站洪流**：一轮窗口内真账号 **48 条公告**，其中 40 条无落点 | **2026-09-18 Steven 拍板走 C** → 决策记于 **ADR-022** | **实测数据（2026-09-18，`npm run probe:announcements`，真账号只读）**：6 门课 48 条公告；落点判定 8 有 / 40 无；对那 8 条跑真解析（同一份 prompt/schema/校验器）→ 只有 `Exam 1 Info`（1 条考试）与 `Quiz 1 Solution posted`（6 条考试）真能写，其余 6 条产出 0 条。**问题**：① 40 条通知类每条都要用户点「知道了」= 48 次操作/轮，与 **ADR-016「用户操作量趋零」直接冲突**；② 粗筛准确率 25%，用户点「确认」后会被明确告知"没有可写入的内容"（不是静默失败，但仍是白操作）。**三个选项**：**A** 按卡面全进（最忠实 §14，但操作量最大）｜**B** 只让"有落点"的进消息栏（8 条/轮，但用户会漏掉 "Office hours 改了" 这类在意的事）｜**C** 有落点逐条进 + 无落点**合并成一条摘要**（"6 门课共 40 条通知"），需要一点新 UI。**选 C 的理由**：消息栏只新增 **9 条**（8 逐条 + 1 摘要）而**一条不漏**（摘要里每条都带原文入口）；A 牺牲操作量（48 次/轮）、B 牺牲完整性（把"office hours 改了"藏起来），C 只牺牲"通知类公告的独立卡片"这个没人需要的形态 |
| #9 提交政策 → 产出 study routine | **Phase 1 · P0-3-21** | 需 modules + syllabus 解析 + LLM 生成，属新能力（Steven 未上移） |
| 课程加权百分比 | **Phase 1 · P0-3-22** | Canvas 权威总分拿不到（`enrollments.grades` 对本人 null）+ 各课加权口径不一，等可靠映射 |
| #10① 大 project 分解 / 顺延 | **Phase 2** | 权重排序 + 任务分解引擎 |
| #10② Google Calendar 接入 | **Phase 1** | `Roadmap.md` §5 已列；Gmail 属 restricted scope，需安全评估 |
| #5 课程卡内部 layout 参考 Data C8 / CS 61A | **并入 3-3 / 3-12** | 是设计参考，不是独立功能 |

### P0-3 执行卡（AI 开工最小上下文）

> 与 P0-0 / P0-1 / P0-2 一样：领 task 时只读对应执行卡 + `TechStack.md` 第 2 节版本矩阵，不必重读全部文档。

