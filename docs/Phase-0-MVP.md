# Phase-0-MVP.md — Phase 0 执行清单

> **本文档是「逐个 check」的对象。** 每个 task 完成后停下，交 Steven 验收；通过才做下一个。
> 需求细节见 [`PRD.md`](./PRD.md)，阶段 Gate 见 [`Roadmap.md`](./Roadmap.md)，开发行为约束见 [`CodingRules.md`](./CodingRules.md)。

---

## 使用方式

**workflow（强制）**
1. Bud 领一个 task → 读下方该 task 的**执行卡**（AI 开工最小上下文，不必重读全部文档）→ 实现 → 自测
2. 交付时报告三件事：**改了什么 / 自测结论 / 下一步**
3. Steven check → 通过 → 下一个 task；不通过 → 原地返工
4. 一个 task 未通过前，**不启动下一个**

**执行卡机制**：每个阶段表格下方附「执行卡」小节，含做什么 / 改哪些文件 / 关键约束 / 验收命令 / 交接点。**交接点 = 需要 Steven 手动操作的步骤**（建账号、填密钥、点按钮等），AI 到交接点就停，不空转等待。已完成的 task 执行卡标注「勿重做」，防止下次会话重做。当前已覆盖 P0-0，后续阶段做到时再补卡。

**编号规则**：`P0-<里程碑>-<序号>`
- `P0-0-x` 基础设施
- `P0-1-x` M1 — 静态理解（syllabus 解析）
- `P0-2-x` M2 — 动态感知（数据源同步）
- `P0-3-x` 验证与收尾

**状态**：⚪ 未开始 ｜ 🔵 进行中 ｜ 🟡 阻塞 ｜ ✅ 已通过 ｜ ⏸ 暂缓 ｜ ⛔ 条件性（可能不需要做）

**Owner**：`Bud` = Bud 实现 ｜ `Steven` = Steven 执行 ｜ `共担` = 需双方配合

---


## 本索引怎么用（2026-09-18 拆分后）

> **本文件已瘦身为「薄索引」**：只留「当前进度 + 逐卡一行表 + 关键路径 + 风险登记」。
> 逐卡详情（执行卡 / 验收留档）已按标题锚点**逐字**搬到 [`docs/cards/`](./cards/README.md)，
> **原文一个字都没改**（由 `scripts/verify-phase0-docs-split.py` 机械校验：字符总量 / 逐卡逐字 / 卡号集合）。
>
> **开工顺序（取代旧的「一次读 6 份 docs」）**：
> ① 读本索引 → ② 读当前卡的 `docs/cards/P0-3-XX.md` → ③ 按
> [`cards/README.md`](./cards/README.md) 里该卡的**依赖清单按需**读其余 Docs →
> ④ `grep -rn "// ?"` **收窄到本卡涉及的 `lib/` 子目录**，不再全仓扫。

| 去向 | 内容 |
|---|---|
| [`cards/progress-pointer-archive.md`](./cards/progress-pointer-archive.md) | 本文件原「当前进度指针」全段（166 行完整留档） |
| [`cards/P0-0.md`](./cards/P0-0.md) | P0-0 执行卡（7 张） |
| [`cards/P0-1.md`](./cards/P0-1.md) | P0-1 M1 执行卡（11 张） |
| [`cards/P0-2.md`](./cards/P0-2.md) | P0-2 M2 执行卡（4 张 🎫 + 6 段二级卡） |
| [`cards/P0-3-overview.md`](./cards/P0-3-overview.md) | P0-3 排序纪律 + 「明确移出 M3」 + 执行卡前言 |
| [`cards/P0-3-XX.md`](./cards/P0-3-1.md) | P0-3 每张卡一个文件（23 个，按 🎫 锚点切） |
| [`Phase-0-changelog.md`](./Phase-0-changelog.md) | 原「变更记录」段（70 行） |

## 当前进度指针（压缩版）

> **完整原文（166 行，含 3-8 ~ 3-20 的逐卡收尾细节）见
> [`cards/progress-pointer-archive.md`](./cards/progress-pointer-archive.md)。**

| 项 | 值 |
|---|---|
| **当前卡** | `P0-3-35` **总览取数：已完成的历史不该吃掉窗口**（🔵 代码完成 + 真库探针实证 2026-09-23，**待 Steven 验收**）。🔴 **freeze 前的显式例外**：数据准确性 bug，不是新功能（Steven 2026-09-22 拍板「质量修复可以插队」）。症状 = 总览页 7 天日历全空、待办只剩考试；根因 = `loadTasks()` 只有时间上界 + due 升序 → 一学期的已交作业吃光 50 条名额（实测：50 条里 35 条历史、**未来 0 条**）。修后：历史 0 条、**未来 14 条**，逾期未完成 1 条不变。见 [`cards/P0-3-35.md`](./cards/P0-3-35.md) |
| **上一张** | `P0-3-34` 成绩速记（✅ **Steven 验收通过 2026-09-21**，第 34 张卡、第二批收官；commit `fb46c7c` + `8ac2305` + `ef07f0c` 已 push，`gh api …/commits/ef07f0c/status` → success / Vercel success）。核心 = **手记分数不被下一轮 Canvas 同步抹掉**：新增列 `tasks.score_source`（`null/canvas/manual`，刻意不 backfill）+ 把「判定」与「写入 patch」收成 `lib/sync/canvas-tasks.ts` 的 `canvasTaskColumns()`（**同源**，原先两份代码加闸必然分叉）；`PATCH /tasks/:id` 加独立 `score` 字段组（准入不看 source）；prompt 升 **v3**（明令禁止从平均分/最高分产出得分）。**关键验收含反向对照组**：播种 `manual` 1.5/2 → 跑真同步仍是 1.5；只去掉标记 → 立刻被改回 Canvas 的 1.25（Chem 1AL 计数 `0/1/0`→`0/0/0`），跑完已还原不留测试数据。生产域名 `PATCH /api/v1/tasks/<uuid>` 无凭证 **401**、同路径 `GET` **405**（对照组）。🟡 **如实标注**：没走真实登录会话跑 PATCH（拿不到 cookie），播种/还原走 service role 直连，验的是同步闸；PATCH 准入由 `regress:scores` 覆盖。门禁 `tsc`/`eslint`/`build` ✓ + **18 条回归全绿** |
| **本批新增（2026-09-19 Steven 拍板）** | `P0-3-29 ~ P0-3-34` 六张 = 「发布前必须补的六件事」，**全部排在 `P0-3-12` 视觉扫尾之前**（3-12 自身要求排在所有 UI 改动之后）。来源：Steven 2026-09-19 六段产品意见，理由「第一个版本不抓住用户，后面发的版本就没人感兴趣」。**🔴 六张已于 2026-09-21 全部验收完毕，不再追加**（配对词表缺口、答题自动判分**均搁置**）。详见 §4 排序说明 |
| **下一张** | `P0-3-12` **全站视觉统一扫尾**（⚪ 未开工；3-35 之后回到它）→ 之后 `P0-3-13` 版本 freeze（**出口门**）。12 的开工第一动作 = 先 grep 找「同一视觉语义存在两套实现」的地方（同类 class / inline style / 重复 badge），**不是先调配色** |
| **里程碑** | M1 ✅ ｜ M2 ✅ ｜ M3 + M3.5 ✅（31 张卡全验收）｜ **M3 第二批 6 张：29 ✅／30 ✅／31 ✅／32 ✅／33 ✅／34 ✅ —— `6/6` 收官** ｜ 🔵 **35（数据准确性修复，freeze 前插队）** ｜ M4 ⚪（进入条件 = `P0-3-13` freeze） |
| **出口顺序** | ~~29~~ ✅ → ~~30~~ ✅ → ~~31~~ ✅ → ~~32~~ ✅ → ~~33~~ ✅ → ~~34~~ ✅ → **`P0-3-35` 取数修复（当前）** → `P0-3-12` 视觉扫尾 → **`P0-3-13` freeze（出口门）** |
| **待验收** | `P0-3-35` 总览取数修复（🔵 代码 + 真库探针已实证，等 Steven 肉眼过一遍总览页） |

**两张未闭合的卡（别在索引里被忽略）**
- 🔵 `P0-3-9`（截图档课程更新对话框）：代码完成 2026-09-13，**真实识别从未验收** ——
  缺 `DASHSCOPE_API_KEY`（Qwen key 手机号未开通）。见 [`cards/P0-3-9.md`](./cards/P0-3-9.md)。
- ✅ `P0-1-11`（解析进度可视化）：**已验收 2026-09-21**（四阶段 stepper + 文本预览；tsc/eslint/build 全绿）；
  卡面只有卡表行，无独立详情卡。见 [`cards/P0-1.md`](./cards/P0-1.md)。

**未开卡的已知缺口（防止「没人记得」）**
- 🔴 同步层 tasks 写入不幂等（每轮 `updated 2`，两条 `graded` + 小数分）——
  ✅ **已开卡 `P0-3-28` 并于 2026-09-18 修完**（真账号连跑两轮，第二轮全零；只读探针 3 → 0）。

## P0-0 基础设施

> 目标：一个能跑、能部署、能登录的空壳。这阶段不产生用户价值，但不做它后面每一步都在泥里走。

| 编号 | 任务 | Owner | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| **P0-0-1** | 项目脚手架：Next.js 16.3.4 App Router + TS + Tailwind v4 + shadcn/ui 初始化，目录结构按 `CodingRules.md` | Bud | — | 本地 `dev` 可跑，访问首页看到占位页；TS 无报错 | ✅ |
| **P0-0-2a** | **【Steven 手动】** 在 Supabase 建项目 + 拿 Project URL / anon key + `cp .env.example .env.local` 填两个值 | **Steven** | P0-0-1 | `.env.local` 已填两个值；`npm run dev` 不报"缺少环境变量"错误 | ✅ |
| **P0-0-2b** | 前后端 client 分离（`lib/supabase/server.ts` / `browser.ts` 分开，不混用）+ `.env.example` 模板 + env 缺失即抛错校验 | Bud | P0-0-1 | `lib/supabase/{server,browser,env}.ts` 就位；service key 不进前端 | ✅ |
| **P0-0-3** | Supabase Auth：邮箱 + 密码注册/登录 + proxy 保护路由 | Bud | P0-0-2a | 可注册、登录、登出；未登录访问受保护页面被重定向 | ✅ |
| **P0-0-4** | 数据库迁移：建表（按 `Database.md` 全部表 + 索引 + 外键） | 共担 | P0-0-2a | 迁移脚本可重复执行；表结构与 `Database.md` 一致 | ✅ |
| **P0-0-5** | RLS 策略：用户只能访问自己的数据 | Bud | P0-0-4 | 用两个测试账号交叉验证，看不到对方任何数据 | ✅ |
| **P0-0-6** | 部署上线（Vercel）+ 生产环境变量 + 冒烟测试 | 共担 | P0-0-3, P0-0-5 | 生产环境可注册登录；无环境变量泄漏 | ✅ |

---

## P0-1 M1 — 静态理解（syllabus 解析）

> 目标：验证"学生愿意上传 syllabus 且认可 AI 解析价值"。**这一阶段零外部依赖、零合规风险，是最该先跑通的部分。**
>
> ⚠️ **表格行顺序 ≠ 执行顺序**：实际先做 `P0-1-7`（课程 CRUD）再做 `P0-1-1`（上传），原因见[进度指针的顺序调整说明](./cards/progress-pointer-archive.md#当前进度指针)。**表格行序保持编号顺序不动，只为编号可读性。**

| 编号 | 任务 | Owner | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| **P0-1-1** | 文件存储：syllabus 上传入口 + Supabase Storage + 类型/大小校验（PDF / docx / pptx） | Bud | P0-0-6, **P0-1-7** | 可上传并取回文件；非法类型被拒绝且有提示 | ✅ |
| **P0-1-2** | 文本提取管线（PDF / docx / pptx），抽不出时**明确降级提示**而非静默返回空 | Bud | P0-1-1 | 三种格式各测一份真实文件；扫描件走降级提示不崩溃 | ✅ |
| **P0-1-3** | LLM provider 抽象层（`lib/llm`）：默认 DeepSeek adapter + JSON schema 结构化输出 + 错误处理 | Bud | P0-0-6 | 抽象层可用；切换 provider 只改配置不动业务代码（[ADR-003](./Decisions.md#adr-003)） | ✅ |
| **P0-1-4** | 五板块抽取 prompt v1：Grade Composition / Course Outline / Test Dates / Office Hours / Submission Policy，**拆成独立抽取任务** | Bud | P0-1-3 | 每板块独立调用；输出符合 schema；缺失字段返回 null 不编造 | ✅ |
| **P0-1-5** | 解析 API 路由 + 结果落库 + **修正 diff 存储**（保存原始解析 vs 修正后 + 差异字段） | Bud | P0-1-2, P0-1-4 | 数据库同时存有原始与修正版本，可追溯差异 | ✅ 5a + 5b 均已验收（2026-09-03） |
| **P0-1-6** | 可编辑表单 UI：五个板块逐项编辑 / 补全 / 覆盖 + 保存 | Bud | P0-1-5 | 可修改任一字段并保存；TBD 状态可正常展示与编辑 | ✅ 已验收（2026-09-03；验收点在详情页，见 P0-1-8） |
| **P0-1-7** | Workspace CRUD：创建（学期 + 课程名必填，编码/教师选填）、列表按学期分组、编辑、删除 | Bud | P0-0-6 | 建课后出现在总览页与列表；删除有二次确认 | ✅ |
| **P0-1-8** | 课程详情页：展示五板块最终信息 | Bud | P0-1-6, P0-1-7 | 保存后的数据完整展示；缺失项显示 TBD 而非空白 | ✅ 已验收（2026-09-03 11:50） |
| **P0-1-9** | 总览页 v1：课程卡片（显示近期 1-2 个任务）+ 跨课程近期任务列表（仅 syllabus 数据，按日期排序） | Bud | P0-1-8 | 能看到"最近 7 天所有课程要做的事"；可跳转课程详情 | ✅ **2026-09-03 验收通过**（本地 64/64 + 生产 26/26）。**M1 收口** |
| **P0-1-10** | 冷启动：**Demo Workspace**（预置示例课程，含已解析 syllabus 与示例任务） | Bud | P0-1-9 | 新用户从进入站点到看到有内容的总览页 ≤ 30 秒 | ✅ **2026-09-04 验收通过**（Steven 浏览器：点「先看看效果」产生 CHEM 1A 示例） |
| **P0-1-11** | 解析过程可视化：上传后先显示文本预览，再逐步填充五个板块 + 进度提示 | Bud | P0-1-6 | 用户不再干等；每一步有明确状态反馈 | ✅ 已验收 2026-09-21（四阶段 stepper + 文本预览；tsc/eslint/build 全绿） |

---

## P0-2 M2 — 动态感知（数据源同步）

> 目标：验证"任务自动同步是否减少打开 Canvas 的次数"——**Tempo 内核的第一环**。
> ✅ **P0-2-1 已于 2026-09-01 完成（Steven 实测）：bCourses 的 "+ New Access Token" 按钮存在且可用** → M2 走 PAT 路线，iCal 兜底（P0-2-10）**不执行**。
> iCal 仍作为长期 Plan B 保留（学校若将来关闭入口则启用），但不再是 Phase 0 的工作项。

| 编号 | 任务 | Owner | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| **P0-2-1** | **Canvas token 可行性实测**：登录 bCourses → Settings → 确认"+ New Access Token"按钮是否可用；记录是否强制填过期时间、上限多久 | **Steven** | — | ✅ **结论：可用**（2026-09-01）。M2 走 PAT；P0-2-10 不执行 | ✅ |
| **P0-2-1b** | **记录实测细节**：是否强制填写过期时间、上限天数、token 可撤销入口位置 | **Steven** | P0-2-1 | 三项信息记录在案（影响 P0-2-8 提醒逻辑的默认值） | ✅ **2026-09-04 完成**：强制必填 / 上限 **90 天** / 撤销在 Access Tokens 同页。已同步进 6 份文档（推翻原"约 120 天"） |
| **P0-2-2** | 凭据加密存储（AES）+ 服务端 Canvas 请求封装（token 绝不出现在前端响应中） | Bud | P0-2-1（可用） | DB 中凭据为密文；抓包确认前端拿不到 token | ✅ **已验收（2026-09-04 10:14）** |
| **P0-2-3** | Canvas API 客户端：拉取课程列表 + 作业列表（含 due date），含限流与错误处理 | Bud | P0-2-2 | 能正确拉取真实数据；API 报错有明确处理不崩溃 | ✅ **已验收（2026-09-04）** |
| **P0-2-4** | 课程关联 UI：手动将 Canvas 课程与 Tempo Workspace 关联（不做自动匹配） | Bud | P0-2-3 | 可选择并关联；关联后状态可见；可解除关联 | ✅ **已验收通过（2026-09-04 11:36，Steven 浏览器验收）** |
| **P0-2-5** | 首次全量同步：Canvas 作业 → `tasks` 表落库（去重 + 更新，不重复插入） | Bud | P0-2-4 | 重复同步不产生重复任务；due date 变更能更新 | ✅ **已验收（2026-09-04 13:44，Steven「效果完美」）· 勿重做** |
| **P0-2-6** | 刷新机制：**打开应用自动同步（T1，主力）** + 手动刷新按钮（T2）+ 后台定时兜底（T3，频率见 `Sync-Strategy.md`） | Bud | P0-2-5 | 打开应用/聚焦标签页自动同步（60s 节流）；手动刷新可用（30s 节流）；定时兜底按配置执行 | ✅ **两拍全部已验收（2026-09-05，Steven）**：第一拍 T1+T2；第二拍 T3（自测 18/18 + 完整循环 13/13 + 生产端到端 200） |
| **P0-2-7** | **同步状态 UI**：显示最后同步时间 + **失败可见性**（失败时展示最后成功时间与失败原因，绝不静默展示旧数据） | Bud | P0-2-6 | 断网/token 失效时显示明确错误提示，而非旧数据 | ✅ **2026-09-05 验收通过**（第 18 张卡） |
| **P0-2-8** | Token 过期提醒：基于**用户实际填写的过期时间**（非写死天数）提前提醒 | Bud | P0-2-2 | 用临近过期的测试 token 验证提醒触发 | ✅ **2026-09-05 晚 验收通过**（Steven 浏览器） |
| **P0-2-9** | 撤销授权入口：一键断开 Canvas 授权 + 删除已存凭据 | Bud | P0-2-2 | 断开后凭据从库中清除；同步停止且状态正确显示 | ✅ |
| **P0-2-10** | ~~iCal Feed 兜底（条件性）~~ | Bud | — | ⏸ **不执行**：P0-2-1 判定为可用，条件未触发。保留为长期 Plan B（[ADR-002](./Decisions.md#adr-002)） | ⏸ |
| **P0-2-11** | 总览页合并展示：syllabus 考试日期 + Canvas 作业 due date 合并排序；考试日期以 `exam_dates` 为权威源 | Bud | P0-2-5, P0-1-9 | 两类任务正确合并；**课程页与总览页日期一致**（[ADR-004](./Decisions.md#adr-004)） | ✅ **2026-09-05 深夜**（核对 28/28，零功能代码，M2 收官） |

## P0-3 打磨收敛期（M3）—— 产出「可发版」

> **M3 的定位（2026-09-13 Steven 拍板）**：只管把版本打磨到**敢发给种子用户**，**不接触真实用户**。验证相关的四张卡已整体下移到 [P0-4 验证期](#p0-4-验证期m4-原-gate-01-内容下移)。
> **🔴 exit gate**：P0-3-13（版本 freeze + 内部试用）之后**不追加卡片**。「想再改一版」归入 Phase 1/2，不插队 —— 打磨没有尽头，而 Fall 2026 验证窗口有截止日。

> **M3 第二批（P0-3-29 ~ 34，2026-09-19 Steven 拍板）**：距 exit gate 一步时 Steven 提出六项「发布前必须补」的意见，
> 理由是**种子用户只有一次机会** —— 第一个版本不抓住用户，后面发的版本就没人感兴趣。这是**他的主动判断**，不是无纪律追加：
> 六张全部**排在 `P0-3-12` 之前**（3-12 自身要求排在所有 UI 改动之后），实施顺序见进度指针的「出口顺序」。
> **🔴 这六张是最后一批**：它们做完即进入 3-12 视觉扫尾 → 3-13 freeze，**之后不再追加**。

| 编号 | 任务 | Owner | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| **P0-3-1** | 量化指标埋点：编辑修正率、7 日回访次数、人均关联课程数、token 续期完成率 | Bud | P0-2-11 | 四项指标可查；数据口径与 `PRD.md` 定义一致 | ✅ 已验收 2026-09-09 |
| **P0-3-2** | 设置与隐私页：数据清单 + 隐私说明 + 一键删 Canvas 凭据 + 一键删账号（级联） | Bud | P0-2-9 | 删除后所有相关数据（含 Storage 文件）被清除 | ✅ 已验收 2026-09-17（Steven 验收通过） |
| **P0-3-3** | **设计基线**：移植 Stride 设计令牌 + 应用外壳（固定侧栏 220px / 吸顶顶栏 76px / 卡片网格 / 深色主题） | Bud | P0-3-2 | 令牌进 Tailwind 配置；外壳在所有页面生效；深色主题可读 | ✅ 已验收 2026-09-13 |
| **P0-3-4** | **解析质量修复**：`L1–L40` 讲座编号课表稳定漏抽（真 bug） | Bud | — | 该格式课表能被抽出 | ✅ 已验收 2026-09-13（回归门 4/4 PASS，commit `ed143f1`） |
| **P0-3-5** | **模块与上传解耦**：空课程直接进五模块，上传降级为模块内可选动作 | Bud | P0-3-3 | 建课 → 直接看到五模块空态 → 可选上传 | ✅ 验收 2026-09-13 |
| **P0-3-6** | **总览页列表收敛**：只列最近 10 条，其余收进 BOX | Bud | P0-3-3 | 与「已完成折叠」+ 7 天窗口语义不冲突 | ✅ 验收 2026-09-13 |
| **P0-3-7** | **总览页可视化**：周历 / 时间线视图 + 考试日期并入主 Calendar + 进度条（**分子分母口径必须先定**） | Bud | —（已解闸：3-10 2026-09-13 完成） | 考试日期在日历可见；进度条口径明确且数据准确 | ✅ 已验收 2026-09-13（commit `6c8b3b2`） |
| **P0-3-7b** | **总览页收纳**：课程卡搬出 dashboard → 新建 `/courses` 列表页 + 侧栏「我的课程」入口（**结构改动，排在 3-12 视觉扫尾之前**） | Bud | P0-3-7 | dashboard 只回答「接下来要做什么」；课程资产独立成页 | ✅ 已验收 2026-09-13（commit `ba361c4`） |
| **P0-3-8** | **课程更新对话框（文本档）**：输入 + AI 解析 + **人工确认** + 只写手动任务 | Bud | P0-3-4 | 对话**不篡改** syllabus 权威数据（exam / 已确认项） | ✅ 用户验收通过 2026-09-13（`45d3bc3` + 3-8b `5969f55` + follow-up `4f86ab7`） |
| **P0-3-8b** | **对话框检索/更新档**：输入后**先检索现有任务** → 0 命中新增 / 有命中列举让用户选改哪条；`PATCH` 放开手动任务内容编辑 | Bud | P0-3-8 | 只有 `source='manual'` 可改（canvas/exam 被拦并引导源头）；匹配确定性、不用 LLM | ✅ 用户验收通过 2026-09-13 |
| **P0-3-9** | **课程更新对话框（截图档）**：多模态接入 | Bud | P0-3-8、~~O-11~~（✅ 已解除） | **验收用例：Chem 1A Homework 6 的 Gradescope 提交成功页截图 → 自动标记完成** | 🔵 **代码完成 2026-09-13；真实识别待 Steven 贴 `DASHSCOPE_API_KEY` 后补验**（手机号暂未开通 Qwen key） |
| **P0-3-10** | **Canvas 提交状态同步 + 日期时区修复**：`include[]=submission` + 三态 + 「待确认」态 + 按学校时区渲染 | Bud | — | 见下方执行卡 5 条验收路径 | ✅ 已完成（2026-09-13 用户验收） |
| **P0-3-11** | **邮件通道·入站（纯入站）**：转发到 Tempo 专属**密址**（token 绑定，不认 `From`）→ 解析（DeepSeek 中国）→ 仅 `status='done'`（绝不自动建任务 / 改 dueDate / 写 `submission_state`）—— 出站归 `P0-3-14` | Bud | P0-3-8 | 转发一封 Gradescope 提交确认邮件 → 任务自动标记完成 | ✅ **代码完成 + 端到端验收通过 2026-09-17**（ADR-019：砍掉注册邮箱绑定 + 「地址已就绪」回信，纯入站；真信 `Lab 2: Smells` 自动标记 done） |
| **P0-3-12** | **全站视觉统一扫尾**：把 3-3 ~ 3-23 全部新界面收进设计基线（**🔴 必须排在所有 UI 改动 3-16 / 3-17 / 3-18 ~ 3-23 之后**——否则扫完又得重扫一遍） | Bud | **P0-3-23** | 无遗留旧样式；对齐 Stride 观感 | ⚪ |
| **P0-3-13** | **版本 freeze + 内部试用**（Steven + 1-2 熟人）｜**出口门** | 共担 | P0-3-12 | 修掉「看不懂 / 不舒服」；产出**可发版** | ⚪ |
| **P0-3-28** | **同步写入幂等（`numeric` 定标）**：`points_possible` / `submission_score` 是 `numeric(10,2)`，Canvas 给未定标浮点（实测 `9.923076923076923` vs 库 `9.92`）→ 差量判定永不收敛 → 每轮 `updated 2` 且 `updated_at` 被无意义刷新。修法 = **写入与比较共用同一个已定标的值**（`roundToScale()`，半值远离零与 Postgres 一致）；`hasChanged()` 改薄封装 `canvasTaskDiffs()`，与在线探针共用同一份判定。**🔴 必须排在 P0-3-13 之前** | Bud | — | **连跑两轮同步，第二轮 `created 0 / updated 0 / deleted 0`** | 🔵 **代码完成 + 本地验收通过 2026-09-18，待 Steven 验收**（真账号 6 门课第二轮全零；只读探针 3 → 0） |
| **P0-3-14** | **主动提醒 / 优先级**：出站推送（提醒时机 + 优先级建议 + 频控）—— **ADR-017 点名的"秘书"核心护城河**（此前无卡） | Bud | P0-3-11 | 见下方执行卡（三项拍板：渠道=邮件 / 优先级=截止临近排序 / 正文=聚焦版） | ✅ **验收通过 2026-09-17**（第 25 张卡）：迁移①/Paid②/发件地址③/Worker④/env⑤/**真发⑥**（生产 `usersReminded:1`，Steven 已收到）；**验收发现「可提醒范围」漏筛 `submission_state`（误报 18/21）已修**并随本次复验通过 |
| **P0-3-15** | **完成态统一 + 提交态徽标**：修 `TaskList`「分组用合并判定、行内渲染只看 `status`」的分叉；Canvas 已判定完成 → 勾选框改为**静态灰勾（不可点）**；任务名后加**六态徽标**（未提交 / 缺交 / 已提交 / 待查重 / 已评分 / 待确认） | Bud | P0-3-14 | ① dashboard 里 Canvas 已判完成的作业不再画空勾选框；② 每条有提交态的任务后都有对应徽标 | ✅ **验收通过 2026-09-17**（第 26 张卡，原话"验收通过，效果非常好"）：`isCanvasDone()` 收口三处副本；真数据 40/83 条受影响；回归 +7 条；commit `cfc34ef` 已 push + 部署 success |
| **P0-3-16** | **Dashboard 精修**：① 对话框 placeholder 改「Hi，有什么 Update 想要告诉 Tempo？」② 删「已到期作业」区块（debt-bar + dashboard 接线 + 死代码清理 + 回归）③ 加「今日任务」= 今日到期 100% + 未来任务按 `1/剩余天数` 切片 + 逾期未完成并入顶部红组 | Bud | P0-3-15 | ① 提示语替换 ② 欠账卡消失且无残留死代码（grep `debtWindow` = 0）③ 今日任务按天切片正确 | ✅ **验收通过 2026-09-17**（`60f0923` 已 push + Vercel success） |
| **P0-3-17** | **课程详情重排 + 抓取信息落地**：① 单列 `max-w-3xl` → 一屏看全 ② `null`(Canvas 不追踪)任务显灰「需手动确认」、移出逾期红+提醒 ③ 点任务名跳 Canvas(`html_url`，加列+映射) ④ 成绩构成→pie chart ⑤ `score/points_possible` bar，**只列已出分的**，折叠进「作业详情」⑥ 分数条**按分段上色**（满分绿 / 90 蓝 / 80 紫 / 70 珊瑚 / 其余琥珀） | Bud | P0-3-10、P0-3-15 | ① 详情页不滚动即看全 ② `none` 类不标红 ③ 点标题外链 Canvas ④ 饼图渲染 ⑤ **只列已出分**（没分的行不出现）⑥ 分数越高色越"好"、最低档**不用红**（红 = 缺交/逾期） | ✅ **验收通过 2026-09-17**（`5435202` + 验收修正 `c285f70` + 上色 `9e61f42`，均已 push + Vercel success） |
| **P0-3-18** | **消息栏**：侧栏新增入口 + **全屏对话页**（现有浮窗对话框的全屏版）；系统消息（如「Canvas 扫到的新 syllabus 与你上传的不同，是否覆盖？」）与用户自己的更新**同一条对话流**；每条提案带「确认 / 忽略」 | Bud | P0-3-17 | ① 侧栏「消息栏」点进全屏对话 ② 系统提案可确认/忽略 ③ 忽略不写、确认才写（ADR-015） | ✅ **验收通过 2026-09-17 晚**：`messages` 建表 SQL 经 Steven 手跑 + 只读探针确认表/RLS；版面改会话式（`3fff155`）＋课程下拉修复（`1cdbe85`），均已 push + Vercel success；Steven 原话"3-18 没问题了" |
| **P0-3-24** | **M3.5 · 对话框可编排 exams / grade_components + 写入器**：解禁 `tasks/parse` prompt 规则 1（现「绝不产出 exam」）；注册 `exam_dates`/`grade_components` applier（复用 3-17 派生链：exam_dates 变更 → tasks 同步）；grade_components 同 source 合计 ≠ 100% 报警（缺口留灰）；`grade_components.source` 枚举加 `'canvas'` | Bud | P0-3-18 | ① Galen Quiz Dates 6 日期全对带 excerpt ② 课程网页成绩构成拆条（Each Midterm 30%×2）+ 合计校验 ③ 写入走确认通道(ADR-015) | ✅ **验收通过 2026-09-17 晚**（原话"现在效果非常完美"）：`405cd37` + `3801dca` 已 push + Vercel success；迁移手跑 + 只读探针确认；在线探针 12/12 |
| **P0-3-25** | **M3.5 · 公告自动进站**：同步时批量端点 `GET /api/v1/announcements?context_codes[]=course_…` + **显式两端日期**（绕过 `end_date` 默认 +28 天陷阱）+ **窗口起点跟锚点走的动态滚动窗口**（常态"当天+昨天"，断更自动回补，14 天上限，ADR-023）；`course_announcements` 去重表 + HTML 清洗（禁 `dangerouslySetInnerHTML`）+ `html_url` 回跳；公告 → 进消息栏（复用 3-18）；**无结构化落点（如 office hours 变更）只给「知道了」回执，不写字段** | Bud | P0-3-24 | ① 公告进消息栏带原文链接 ② 有落点(quiz/exam 变更)→可确认提案 ③ 无落点→「知道了」④ 前置修订 Sync-Strategy §14 | ✅ **已上线**（`002522b` + C 口径 `07d810a` + 窗口收窄/课程色 `2bedddd`，Vercel success；迁移已跑 + 探针确认）。探针（2026-09-18，真账号）量出 48 条公告 / 40 条无落点 / 8 条判有落点但**只有 2 条真能写** → **Steven 拍板走 C（ADR-022）**：有落点逐条进 + 无落点合并成一条摘要；消息栏只新增 **9 条**（8 逐条 + 1 摘要）而一条不漏。**2026-09-18 晚追加验收修正（ADR-023）**：窗口收窄（锚点驱动，实测 48 条 → 5 条 → **真正新增 0 条**）+ 课程彩色徽标 | ✅ **Steven 验收通过 2026-09-18**（原话"Nice Job，没有问题了，验收通过"）|
| **P0-3-25b** | **M3.5 · 公告要点（AI 总结）**：给消息加中文要点，**不进同步路径** —— 懒生成 + 落库缓存（`message_summaries`，key `(message_id, locale)`）；打开消息栏后 `POST /api/v1/messages/summaries` 静默补齐；**只对仍待处理的公告消息**生成；覆盖不全如实标（"基于最新 20 条 / 共 40 条"）；失败落 `failed` 行 = 不再重试；**必须标「AI 总结」+ 保留「原文 ↗」**；语言是入参（先中文、course code / 考试名保留英文，英文版加一行数据即可） | Bud | P0-3-25 | ① 打开消息栏后公告气泡出现中文要点（标「AI 总结」+ 原文链接）② 刷新后**立刻**就有（缓存命中，不重算）③ 摘要消息标出覆盖率 ④ 模型挂了消息栏照常可用 | ✅ **Steven 验收通过 2026-09-18**（原话"Nice Job，没有问题了，验收通过"）。交付 `08670d0`（已 push + Vercel success）；迁移 `20260920000000` 经 Steven 手跑 + 只读复核（表在 0 行 / **anon key 打 RLS 空返回**）。门禁 `tsc` ✓ / `eslint` 0 error / `build` ✓（新路由与 `[id]` 无冲突，无凭证探针 **401 = 已上线**）；回归 `regress:message-summaries` **89 断言** + `regress:messages` **83**；在线探针 `probe:message-summaries`（真模型零写入）池化 40 条 → 4 条要点 / 单条精确 / **0 编造日期** / 0.86–1.34s。⚠️ 验收前提：3-25 的 9 条消息已全被处理完 → 没有 pending 就没有生成入口，Steven 已把两条改回 pending（`6bdfb787` 摘要 / `c8cf930c` 单条） | ✅ **Steven 验收通过 2026-09-18** |
| **P0-3-26** | **M3.5 · 回执 + 撤销**：确认时把**实际写入的行 id** 存进 `payload.applied`（`{examDateIds?, gradeComponentIds?}`，不是变更前快照）+ `finalizeConfirmation` 落 `decided_at`(确认时刻) + `payload.receipt`(人话回执文本)；反向 undoer 按 id 精确回滚（`UNDOERS` 对称于 `APPLIERS`，`exam_dates` 删行后重跑 `syncExamToTask` 清孤立派生任务；`grade_components` 直接按 id 删）；`messages.status` 加 `'undone'` **终态**（枚举扩展过 CodingRules §10.1 第 16 条：四处同改 types/message.ts / lib/messages/registry.ts / SQL CHECK / regress）；**撤销窗口 24h**（超窗只显历史、不可撤销）。⚠️ **与 3-25b 的交互已想清**：要点只在 `status='pending'` 显示 → 撤销后是 `undone`（非 pending），故**不恢复要点显示**；但缓存行 `message_summaries` 不会被撤销删掉，将来若改回 pending 命中即秒出；`'undone'` 算"已处理"，`canAccept`/`summaryPending` 已排除它 | Bud | P0-3-25、P0-3-25b | ① 确认后出回执含原文链接 ② 24h 内可一键撤销还原 ③ 超窗不可撤销 | ✅ **已上线（2026-09-18）**：`684607e` 已 push + Vercel success；迁移 `20260918000000_message_undo.sql` 经 Steven 手跑 + `probe:schema` 确认（`undone` 被接受 / 对照组仍被拦 / 零残留）；无凭证探针 `PATCH /api/v1/messages/:id` → **401 = 已上线**、对照组 → 404。✅ **Steven 验收通过 2026-09-18**（原话"验收通过"；撤销端到端走 `c8cf930c`） |
| **P0-3-27** | **M3.5 · URL 自动抓取**：贴链接 → 抓首页 → 取同源 `<a>` → 跟一层子页（≤5）→ 合并文本 → 复用 3-25 清洗；护栏：仅 http(s)/超时 5s/≤1MB/≤5 子页/**禁内网 SSRF**（169.254/10./127./172.16-31/192.168）；合并文本 → 复用 3-24 parse 通道自动设 course section card。**三项拍板（2026-09-18）= A/A/A**：① 入口 = 对话框输入框**自动识别 URL**（零新 UI，复用 3-24 通道）；② `POST /api/v1/ingest/url` **只返回文本**（解析仍走现有 `/tasks/parse`，不新开解析路径）；③ 子页用**关键词启发式**（本地确定性、零 LLM） | Bud | P0-3-25、P0-3-26 | ① math53 berkeley 链接跟子页设 section card ② 内网地址拒绝 ③ 无 PDF 也能建卡 | ✅ **Steven 验收通过 2026-09-18**（已上线 `e372424` + `a49f9bc`，Vercel success；无凭证探针 `POST /api/v1/ingest/url` → 401 = 已上线）；代码完成 + 门禁全绿（tsc / eslint 0 error / build / `regress:url-ingest` **67**）；零迁移（复用 3-24 通道 + 3-25 清洗）；端到端：粘贴链接 → 抓取 → 解析 → 确认 |
| **P0-3-19** | **资料索引（Canvas 文件元数据）**：同步抓 `/files`+`/folders` **元数据** → `course_files` 表 → 课程页「资料」区按 Canvas 文件夹分组 + 外链回 Canvas；**只存目录不下载内容** | Bud | P0-3-27 | 资料按 Canvas 结构分组展示；外链回 Canvas；403 受限文件跳过不挂 sync | ✅ **Steven 验收通过 2026-09-18**（原话"可以了"）。代码 `faf6323` 已 push + Vercel success；迁移 `20260921000000` 经 Steven 手跑，**且不信面板 Success、逐条打真接口复核**（新列 / 新表 / 去重键 / anon 不越权，四查全过）；本地用**真实同步入口**代跑 → **6 门课 271 个文件**落库（18.3s，MATH 53 未开 Files 区 → `coursesSkipped 1`）；**幂等复验**（连跑两次，第二次 `0/0/0`）；**以真实用户会话读 271 行**（补 RLS 的另一半）；验收标准 ①②③ 全部在库内真实行上命中 |
| **P0-3-19b** | **资料索引 · 文件夹树 + 一键总结**（3-19 验收后的两项修改）：① 「一股脑全列出来」→ **按 Canvas `full_name` 路径还原成文件夹树**（原生 `<details>` 递归、自然序 `Unit 2 < Unit 10`、根目录文件置顶、20 个以上默认折叠）② 每个文件旁加**「一键总结」**→ 跳 `/courses/[id]/files/[fileId]` 生成 HTML 页（`<Suspense>` 流式 + 懒生成落库缓存）；**索引路径仍零下载**，按需路径只读那一个文件、**原文不落库**；图片/音视频/压缩包在**发请求前**判 `unsupported` | Bud | P0-3-19 | ① 资料按老师建的文件夹分层展示 ② 每个可总结的文件旁有入口、点开出 HTML 总结页（标「AI 总结」+「原文 ↗」）③ 点不可总结的类型不出页面请求 | ✅ **Steven 验收通过 2026-09-18**（原话"验收没问题了，通过"；`015dc46` 代码 + `3069376` 复核，均已 push + Vercel success）。迁移 `20260922000000_file_summaries.sql` 经 Steven 手跑，**且不信面板 `Success`、逐条打真接口复核**：`probe:schema` 12/12（含 `locale='fr'` 反向断言）+ 13 列点名 + **真跑写入路径**（`ready/cached=false` → 再调 `cached=true`）+ **RLS 双向**（anon 读 0 行 / 真实用户会话读 1 行）。见 **ADR-026** |
| **P0-3-20** | **大纲漂移检测**：同步**零下载**只挑出 syllabus 文件（复用 3-19 的 `course_files` 元数据）→ 打开消息栏时**懒补 diff**（抽文本 → 与已确认五板块 + `exam_dates` 比对）→ 差异**提案进 `P0-3-18` 消息栏**（**不自动覆盖**） | Bud | P0-3-19、P0-3-27 | 抓到 syllabus 变化 → 出差异提案 → 确认才写 | ✅ **Steven 验收通过 2026-09-18**（`6fe260e` 代码 + `3908dcd` 迁移注释修正，均已 push + Vercel success）—— `regress:syllabus-drift` 106/0 + `tsc`/`eslint`/`build` 全绿 + 真账号只读探针跑通；**迁移 `20260923000000_syllabus_drift.sql` 已跑 + 两条腿复核生效**（截图 `FOREIGN KEY → course_files` + 探针「✓ 两列都在」）。验收用**真实入口代跑两轮 + 零残留播种**走完整链（标准 ①「新增 X / 变动 Y」→ 确认 → 写入 → 撤销还原全覆盖，**全程不碰真实行**）。⚠️ 仅「模型从一份真变过的大纲里读出差异」一环未被真实数据验过 |
| **P0-3-23** | **M3 · practice test 生成（类型 A 忠实自测）**：用户在资料区选 past exam（**自动配对**它的 answer key，也可手动换）→ 只读这两份 → 切出题面、**答案默认收起**（`<details>`）→ 逐题可要「讲解这道题的解法」→ 结果进 3-18 消息栏 | Bud | P0-3-19、P0-3-27 | 生成自测卷；**不幻觉出新题**（类型 B 不做） | ✅ **Steven 验收通过 2026-09-18**（原话"验收没有问题了"；`e00710d` 代码 + `8060656` 迁移复核 + `48a7120` 收尾共享面五处 + `4f62e64` 坑索引，均已 push + Vercel success；迁移 `20260924000000_practice_tests.sql` **已由 Steven 手跑 + 零残留探针五查全过**）。门禁：`tsc` ✓ / `eslint` 全仓 0 error / `build` ✓（新路由 `/courses/[id]/practice-tests/new` 在路由表里）/ `regress:practice-tests` **145 通过 0 失败** / `regress:messages` **108 通过 0 失败**。见 **ADR-027** |
| **P0-3-29** | **考试写入去重：改期语义 = 更新提案**：🔴 现状 `lib/course-update/apply.ts` 的 `insertExams()` **只会 insert、从不查已有行** → 公告说「Midterm 1 改期到 9/27」会**并列多出一条 `exam_dates`**。本卡在共享写入器加「先解析它落到哪一行」：同课程按考试名归一命中 → `kind='update'` 提案（带旧值 / 可撤销），确无命中才新增 | Bud | P0-3-24、P0-3-25、P0-3-26、P0-3-20 | ① 改期公告出「更新 9/28 → 9/27」而非「新增」② 确认后行数不变、日期变、派生任务跟着变 ③ 24h 内撤销写回旧值且行还在 ④ 跨课同名不串 ⑤ 连跑两轮幂等 | ✅ **Steven 验收通过 2026-09-19**（上线后两轮实测跟进：归一化补 `test ≡ exam` 词级同义折叠，`dd17f57` 已上线）。修法 = 新增零依赖纯模块 `lib/course-update/exam-match.ts`（`normalizeExamName` / `resolveExamTargets`：同课内按名字归一命中，唯一命中 = `update`、多命中 = `ambiguous` 列出来让人挑、通称不可辨识 = `unidentifiable` 不许悄悄新增）；`apply.ts` 的 `insertExams` → `writeExams`（命中即 update + 留 `applied.examRestores` 旧值快照，**不碰 `source` / `is_confirmed` / `exam_name`**）；公告侧新增懒补端点 `POST /api/v1/messages/exam-proposals`（打开消息栏时算好 `before → after` 写进 `payload.details`，确认时**复用同一份结论**、不再解析）；`undoers/announcement.ts` 补 restore 分支（改期撤销 = 写回旧值，**不是按 id 删**）；对话框用同一份纯函数渲染「更新 9/28 → 9/27」/「新增」/「待你指定」。门禁：`tsc` ✓ / `eslint` 0 error / `build` ✓（新路由在路由表里）/ `regress:course-updates` **68/0** / `regress:messages` 108/0 / `regress:announcements` 95/0 / `regress:syllabus-drift` 106/0。**零迁移**（新增字段全在 `payload` jsonb 里）  |
| **P0-3-30** | **syllabus 两个来源并存**：3-19 已能挑出 syllabus 文件但只当漂移锚点、从不解析 → 用户还得手动再传一遍。**保留上传按钮**（Steven 2026-09-19 明确要求），**并排**加「从 Canvas 资料选一份」→ 走按需路径拉取 + 复用 P0-1 解析管线（原文不落库）→ 落 `source='syllabus'` + 考试 `is_confirmed=false` → 锚点设 baseline | Bud | P0-3-19、P0-3-19b、P0-3-20、P0-1-5 | ① 上传入口行为与改动前完全一致 ② 选文件一键后五板块 + 考试进库且溯源正确 ③ 重复点击幂等（零请求）④ 已有 syllabus 的课**不被自动改** ⑤ 同步路径 Canvas 请求数不变（ADR-026）⑥ 导入后不发漂移提案 | ✅ **Steven 验收通过 2026-09-19**（九条标准全过；`09ec69b` + `61521db` 已上线） |
| **P0-3-31** | **考试复习模式 v1**（**仅限归类为考试的**）：以 `exam_dates` 行为入口的 `/courses/[id]/exams/[examId]/review` —— 考试信息 + 推荐已索引文件（可勾选）+ **上传额外文件**（Steven 确认含）→ 生成**本次考试的总结**（多文件合并）/ 挑 past exam 走 3-23 出卷 | Bud | P0-3-19/19b、P0-3-23、P0-3-29 | ① 复习页列出推荐文件 + 可上传 ② 总结可复用缓存 ③ 无 past exam 时「试卷」按钮不可用且有说明（**绝不降级成 LLM 出新题，ADR-027**）④ 跨用户隔离 ⑤ 非考试任务无入口 | ✅ **Steven 验收通过 2026-09-20**（原话"可以了没问题了，验收通过"）：五条逐条走真界面；④ 由 `probe:exam-review` **六查实证**（关键 = 「传到别人 uid 段 → 403 AccessDenied」+ 表侧 RLS 两半 anon 0 行 / 用户会话 1 行）。迁移 `20260926000000_exam_review.sql` 已跑，**不信面板 Success**，逐列点名 + 枚举 CHECK 带对照组复核；私有桶 `exam_review` + 4 条 `storage.objects` 策略 Dashboard 手建（**git 看不见 → 故新增 `probe:exam-review` 行为验证**）。代码 `0da0e84` / `556d108` / `653ebd0` 已 push + Vercel success，无凭证探针两条新 API → 401。门禁：`tsc` 0 / `eslint` 0 error / `build` ✓ / **16 个回归全绿**（新增 `regress:exam-review` 55/55）。**零新增 message 类型**。🔴 复习数据键 = `(course_id, exam_key)`（非 `exam_dates.id`），见卡面。⚠️ 遗留：`paper-entry.tsx` 未接 `answerKeyFileId` |
| **P0-3-32** | **新手引导 + 侧栏反馈链接**：① 新用户首次登录进入**可左右翻的教程卡**（配 Canvas access token 全流程，每步带跳转链接，可跳过 + 可重看）② 侧栏底部 `Course OS v0.1` 下方加**反馈链接**（**占位符常量**，Steven 之后给 Google Form 替换） | Bud | P0-1-10 | ① 首次登录出引导且可左右翻 / 跳过 ② 外链新开页且只放行 http(s) ③ 二次登录不再出现 + 有「重看教程」入口 ④ 反馈链接改一个常量即全站生效 ⑤ Demo Workspace 入口仍在 | ✅ **Steven 验收通过 2026-09-20**（原话"好没问题了"；`33a9900` 卡片组 + `b216275` 动图 + `39a533b` ② 补图与文案纠错，均已 push + Vercel success；四条静态资源生产站 200）。**零迁移零新环境变量**（占位常量除外）：引入三个零依赖模块 `lib/constants.ts`（`FEEDBACK_URL`）/ `lib/safe-url.ts`（`readSafeUrl` 唯一实现，`lib/messages/view.ts` 弃用私有副本）/ `lib/onboarding/content.ts`（四张卡文案 + `hasSeenOnboarding()`）。「看过」标记用 **cookie 且值 = userId**（服务端读得到 → 引导进首屏 HTML 不闪；判据是**相等**而非存在 → 同机换账号照常出现，这才叫每用户）。`regress:onboarding` **69/0**（含 `fs.existsSync` —— 视频文件名打错时页面与构建都不报错，只留一个白框）。**演示动图**：三条原始录屏是 3024×1898 全屏 → 按隐私边界裁切后压到 77 + 495 + 143 KB（`public/onboarding/`，原 `.mov` 未入库），见卡面「②③的三条留档」 |
| **P0-3-33** | **视觉区分强化**：① 考试视觉语言全站统一（周历 pill / 今日任务行 / 课程详情同一套「边框 + 底色 + 图标」，不再只靠一个「考」字）② 课程色从圆点扩到课程卡色条 / 任务来源标记 / 考试 pill ③ 登录页水印 `Build by Youcheng Li · UC Berkeley` ④ 顺手：`material` 空写入按钮「确认」→「知道了」（3-23 遗留，需同步改 `regress-messages.ts` 钉住它的断言） | Bud | P0-3-23、**P0-3-29**（先修语义再谈怎么画） | ① 同一场考试三处同款视觉 ② 课程色全站一致 ③ 产物 CSS grep 得到新类（Tailwind v4 陷阱）④ 水印只在登录页 ⑤ `material` 按钮文案改且回归全绿 | ✅ **Steven 验收通过 2026-09-20**（原话"验收没有问题。进入0-3-34"；commit `00d7c0a` 已 push + Vercel success，**生产域名实测** `/login` / `/signup` 200 且命中水印）。改动 **15 改 + 2 新建**（+310/−71），**零迁移**（只多 select 一列既有 `task_type`）。四处收口：考试视觉唯一出处 `components/tasks/exam-mark.tsx`（「考」字保留 + `aria-hidden` + `sr-only`「考试」，**判定仍只走 `isExamTask()`**，零新增判定）、课程色唯一出处 `lib/courses/course-color.ts` 的 `COURSE_COLOR_CLASSES`（**静态字面量** `bar`/`chip`/`edge`，取代内联 style 圆点 → Tailwind v4 扫得到）、水印 `components/auth/build-signature.tsx`（只挂 login + signup）、`material` 空写入按钮统一「知道了」。门禁：`tsc` ✓ / `eslint` 0 error / `build` ✓ / **17 条回归全绿**（`regress:messages` 109/0）；产物 CSS 用 `-F` grep `bg-*`×5 / `bg-*-bg`×5 / `text-*`×5 / `border-l-*`×5 / `bg-lime/10` / `sr-only` 全部命中；**真实计算样式探针**（`next start` + Chrome headless `--dump-dom`）确认左边色不被四面边框盖掉。⚠️ 唯一观感变化 = 周历考试 pill 由实心 lime 改浅底描边（Steven 已接受）；消息栏课程色未动（`courseToneClass` 按课程名哈希 → 与课程卡可能不同色，待需时开小卡） |
| **P0-3-34** | **成绩速记（最小版）**：用户在对话框贴「Discussion quiz 得分 9.5/10」→ 解析 + 复用 `matchTasks()` 列候选让用户挑 → 写现有 `tasks.points_possible` / `submission_score` + 标完成 → 课程卡画出与其他作业同款的**成绩条**。🚫 **明确不做**独立成绩单 / GPA 视图（Steven 选最小版，避免撑大 Phase 0） | Bud | P0-3-8b、P0-3-17、P0-3-28 | ① 写入两列 + 标完成 ② 成绩条与 Canvas 来的那条同款分段上色 ③ **🔴 再跑一轮真实同步，手工分数还在**（漏这道闸，下一轮 Canvas 的 `null` 会把用户填的抹掉且无任何提示）④ 无匹配不猜 | ✅ **Steven 验收通过 2026-09-21**（原话"验收过了，这版效果太好了，就是我想要的"；`fb46c7c` + `8ac2305` + `ef07f0c` 已 push，Vercel success）。落地点：**新增列 `tasks.score_source`**（`text` + `CHECK (null/canvas/manual)`，刻意不 backfill，迁移 `20260927000000` Dashboard 手跑）—— 🔴 卡面原写的 `tasks.meta.scoreSource` **不存在**，此项**做不到零迁移**，卡面已更正；加闸落在 `lib/sync/canvas-tasks.ts` 的 **`canvasTaskColumns()`**（原「判定」与「写入 patch」是两份代码，加闸必然分叉 → 收成一份、判定与写入同源，`regress:sync` 的「其他列变了也不带分数列」专守此）；`PATCH /tasks/:id` 加独立 `score` 字段组（准入不看 `source`，与 `status` 同类），校验器 `lib/tasks/score.ts`（`roundToScale()`，缺分子/满分整条拒收）；prompt 升 **v3**（明令禁止从「平均分/最高分/班级平均/占比」产出得分）。**验收③⑤用真账号 + 反向对照组**：Chem 1AL 选 Canvas 已登 1.25/2 的行 → 播种 `manual` 1.5/2 → 跑两轮真同步仍 **1.5** ✅；只去掉标记 → **被改回 1.25** ✅（计数 `0/1/0`→`0/0/0` 精确命中该行），跑完**已还原不留测试数据**。生产 `PATCH /api/v1/tasks/<uuid>` 无凭证 **401**、同路径 `GET` **405**（对照组）。prompt v3 真跑 **4/4**（含负例「平均分」→ scores 空 + 主动进 warnings）。门禁 `tsc` ✓ / `eslint` 0 error / `build` ✓ / **18 条回归全绿**（新增 `regress:scores` 19 条）。🟡 **如实标注**：未走真实登录会话跑 PATCH 的 `score` 字段组（拿不到 cookie），播种/还原走 service role 直连，验的是同步闸。⚠️ 已知边界：派生考试任务上的手记分数在 syllabus 重解析后会丢（与 `P0-3-31` 同源）；截图档 `/tasks/parse-image` 不识别分数（本卡不扩面） |

| **P0-3-35** | **总览取数：已完成的历史不该吃掉窗口**（🔴 freeze 前的**显式例外** —— 数据准确性 bug，不是新功能；Steven 2026-09-22 拍板插队）：`loadTasks()` 只有时间**上界** + `due_date` **升序** → 一学期的已交作业整队占满前 50 条 → 未来任务**一条都取不回**（实测 50 条里 35 条是历史、未来 0 条）。修法 = 只给**已完成**的行设下界（未完成含逾期一律保留），下界按学校日历日回退 7 天；顺带 `CANVAS_DONE_STATES` 提成常量（SQL 与界面同源）、安全上限 50→200、溢出文案改如实、已完成盒标签改「最近已完成」 | Bud | 无（读逻辑） | ① 新取数里"已完成且早于下界"= 0 ② 单跑任一条 filter 都还剩一些、合起来 = 0（证多次 `or()` 是 AND）③ 未来任务数不减 ④ 逾期未完成任务数不减 | 🔵 代码完成 + 真库探针实证（2026-09-23：`probe:task-window` 旧 35 历史 / **0 未来** → 新 0 历史 / **14 未来**，逾期 1 不变）；**待 Steven 验收** |

---

## P0-4 验证期（M4）—— 原 Gate 0→1 内容下移

> 原 P0-3-3 ~ P0-3-6 整体平移并重编号。**进入条件：P0-3-13 版本 freeze 完成。**

| 编号 | 任务 | Owner | 依赖 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| **P0-4-1** | **种子用户招募（5-6 人）**：技术型 2-3 人 + 非技术型 2-3 人，覆盖 ≥2 门不同学科 | **Steven** | P0-3-13 | 名单确定且构成符合要求（**构成不对会导致 Gate 0→1 结论不可信 —— 这张卡要早打招呼**） | ⚪ |
| **P0-4-2** | 种子用户验证跑通：完整旅程 + 两条异常路径（扫描件 / token 失效） | 共担 | P0-4-1 | 每位用户走通主旅程；异常路径均有正确表现 | ⚪ |
| **P0-4-3** | 反馈收集与整理（含定性"是否减少了打开 Canvas 次数"） | 共担 | P0-4-2 | 反馈整理成结构化结论 | ⚪ |
| **P0-4-4** | **Gate 0→1 评审**：对照 `Roadmap.md` 的 G0-1 ~ G0-8 逐条判定 | 共担 | P0-4-3 | 逐条给出达标/未达标结论 + 未达标的处置方案 | ⚪ |

> 🔵 **M3 起每张卡交付必附「给 Steven 的三行」**（2026-09-05 定）：点名 ≤2 个文件 + 一个反直觉的点 + 可点的验收路径。
> **格式与四条硬约束见 `CodingRules.md` §6.1（唯一权威定义，此处不复制）。**
> 开工前另做一件事：`grep -rn "// ?" --include=*.ts --include=*.tsx` 扫 Steven 留下的提问标记，先作答再写卡。


## 关键路径与并行建议

```
P0-0 基础设施
  └─> P0-1 M1 静态理解 ──────────────┐
                                     ├─> P0-2-11 合并总览 ─> P0-3 验证
  ✅ P0-2-1 token 实测（已完成：可用）│
  └─> P0-2 M2 动态感知（PAT 路线）───┘
```

**关于 Phase 1 的 OAuth 申请（Steven 已拍板）**

不提前启动，等 Phase 0 有验证结果后再申请。理由：申请需要机构审批，而 Phase 0 的结论可能改变申请的策略与说辞——带着真实用户数据去谈，比空手去谈更有筹码。

> ⚠️ **这条偏离了原计划**（原建议并行启动以节省时间），属于 Steven 的主动取舍。代价是 Phase 1 的 OAuth 可能赶不上 Fall 2026 学期。**若 Phase 0 验证结果积极，应在 Gate 0→1 评审通过后立即启动申请**，不要继续拖。

---

## 风险登记（执行层面）

| 风险 | 影响 | 触发处置 |
|---|---|---|
| ~~bCourses 关闭学生 token 入口~~ | ~~M2 改走 iCal，功能降级~~ | ✅ **已排除**（P0-2-1 实测可用）。iCal 保留为长期 Plan B |
| LLM 解析效果不达标 | 编辑修正率高但用户仍觉得不准 | 切 Claude provider（抽象层已备，[ADR-003](./Decisions.md#adr-003)） |
| 扫描件 syllabus 比例超预期 | 用户频繁撞到降级提示 | 评估是否启用多模态 provider（待决 O-03） |
| 学期过半导致验证窗口收紧 | 数据样本不足 | 优先保 P0-1（价值峰值在期中前），M2 可适度后延 |

---


## 详情见 `docs/cards/`

- **逐卡索引**（卡号 → 文件 → 状态 → 该读哪几份 Docs）：[`docs/cards/README.md`](./cards/README.md)
- **阶段整段归档**：P0-0 / P0-1 / P0-2 / P0-3-overview 各一份（见上方「去向」表）
- **历史变更记录**：[`docs/Phase-0-changelog.md`](./Phase-0-changelog.md)
- **进度指针全量**：[`docs/cards/progress-pointer-archive.md`](./cards/progress-pointer-archive.md)

> 本索引与 `docs/cards/` 由 `scripts/split-phase0-docs.py` 机械生成（只搬家不重写），
> 由 `scripts/verify-phase0-docs-split.py` 校验。
