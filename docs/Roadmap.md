# Roadmap.md — Tempo 全局路线图

> **文档边界**：本文件是 **Phase 0-4 的全局看板**——管"各阶段什么状态、什么依赖、什么条件下进入下一阶段"。
> Phase 0 的**逐条 task 清单**在 [`Phase-0-MVP.md`](./Phase-0-MVP.md)；需求细节在 [`PRD.md`](./PRD.md)；战略方向在 [`Tempo_产品总蓝图.md`](./Tempo_产品总蓝图.md)。
> 本文件不列具体 task 编号，只管阶段级的 Gate 与依赖。

---

## 1. 状态定义

| 状态 | 含义 |
|---|---|
| 🔵 **进行中** | 当前正在推进的阶段 |
| ⚪ **未开始** | 尚未启动 |
| 🟡 **阻塞** | 已启动但被外部依赖卡住（如机构审批） |
| 🟢 **已完成** | 通过 Gate 验收 |
| 🔴 **已放弃** | 明确砍掉，需记录原因 |

---

## 2. 看板总览

| Phase | 名称 | 核心命题 | 状态 | 进入下一阶段的 Gate |
|---|---|---|---|---|
| **0** | 感知闭环的最小验证 | 学生认不认这两件事的价值 | 🔵 进行中 | 见 [Gate 0→1](#gate-0→1) |
| **1** | 正式动态感知层 | 能不能让更多学生无痛接入 | ⚪ 未开始 | 见 [Gate 1→2](#gate-1→2) |
| **2** | 规划智能层 | 学生会不会照着 Tempo 的计划行动 | ⚪ 未开始 | 见 [Gate 2→3](#gate-2→3) |
| **3** | 轻量代理层 | 敢不敢让 Tempo 替你动手 | ⚪ 未开始 | 见 [Gate 3→4](#gate-3→4) |
| **4** | 生态与资源层 | 能不能从工具变成平台 | ⚪ 未开始 | — |

---

## 3. 内核能力成熟度阶梯

Tempo 的内核是「实时检测 → 动态管理 → 动态规划」。这张表说明**每个 Phase 结束后，内核应该达到什么水平**——它是比功能清单更重要的进度衡量标准。

| 能力维度 | Phase 0 | Phase 1 | Phase 2 | Phase 3 |
|---|---|---|---|---|
| **数据源覆盖** | Canvas 一条（+ iCal 兜底） | Canvas OAuth + Google Calendar + 网页兜底 | 同 Phase 1 + 完成状态追踪 | 同 Phase 2 |
| **同步触发** | 手动刷新 + 宽松定时轮询 | 自动定时轮询 + 变更检测 | 同 Phase 1 | 同 Phase 1 |
| **同步延迟** | 分钟级（前期频率放宽） | 分钟级，按成本优化 | 分钟级 | 分钟级 |
| **变更处理** | 覆盖式更新 | **增量同步 + 变更检测** | 同 Phase 1 + 冲突识别 | 同 Phase 2 |
| **进度感知** | 有 deadline，无完成状态 | 有提交状态 | **追踪完成状态** | 同 Phase 2 |
| **规划能力** | 按时间顺序排列 | 同 Phase 0 | **权重排序 + 任务分解 + 顺延** | 同 Phase 2 |
| **代理能力** | 无 | 无 | 无 | **B1 类写操作** |

> **判断原则**：功能可以做加法，但内核能力**不能跳级**。若 Phase 1 的增量同步没做好就去做 Phase 2 的规划，规划的输入是脏数据，做出来只会"看起来聪明实际很蠢"。

---

## 4. Phase 0 — 感知闭环的最小验证

**状态**：🔵 进行中 ｜ **详细 task 清单**：[`Phase-0-MVP.md`](./Phase-0-MVP.md)

**核心命题**：学生认不认这两件事的价值——① AI 解析 syllabus 省不省心；② 任务自动同步有没有减少打开 Canvas 的次数。

**里程碑**

| 里程碑 | 范围 | 依赖风险 |
|---|---|---|
| M1 — 静态理解 ✅ **已达成 2026-09-03** | 账号 + Workspace + syllabus 解析 + 总览页（syllabus 数据） | 低 |
| M2 — 动态感知 ✅ **收官（11/11 全部验收，2026-09-05 深夜）** | Canvas 数据源同步（**PAT 路线，已确认可用**）+ 凭据管理 + 总览页合并展示 | 低（token 入口已实测可用） |
| M3 — 验证与收尾 🔵 **进行中（2026-09-07 开工）** | P0-3-1 指标埋点 **✅ 已验收 2026-09-09** → P0-3-2 设置与隐私页（🔵 代码完成，待 Steven 浏览器验收）→ P0-3-3 种子用户招募（Steven）→ P0-3-4 验证跑通 → P0-3-5 反馈整理 → P0-3-6 Gate 0→1 评审 | 中（种子用户构成决定结论可信度） |

> **里程碑状态**：M1 ✅ 达成（P0-1-9 于 2026-09-03 16:10 通过 Steven 浏览器验收）。**冷启动前置卡 P0-1-10 Demo Workspace 已于 2026-09-04 09:18 验收通过**（点「先看看效果」产生 CHEM 1A 示例，M1 链路 + 冷启动全部打通）。
> **M2 ⚙️ 进行中（2026-09-05）**：P0-2-1（token 入口实测）/ P0-2-2（凭据加密 + 请求封装）/ P0-2-3（课程列表代理）/ P0-2-4（课程关联 UI，11:36 浏览器验收）/ **P0-2-5 首次全量同步（2026-09-04 13:44 Steven 验收通过，「效果完美」）** 五张卡收口 —— Tempo 第一次自己从 Canvas 拿到了数据：`POST /api/v1/sync/now` + 同步编排（串行 / 重试 / 熔断 / 状态落库）+ 作业去重落库 + 关联后自动触发一次。真实数据实测 CHEM 1A 25 条 / PHYSICS 7A 6 条 / MATH 53 LEC 0 条，一趟三门课串行 6.0 秒；重复同步零写入，用户标记的「已完成」不被覆盖。**P0-2-6 刷新机制 ✅ 两拍全验收（2026-09-05 傍晚，Steven）—— M2 推进到 7/11**：第一拍 T1（打开应用/聚焦标签页自动同步，60s）+ T2（手动同步按钮，30s）浏览器验收通过；第二拍 **T3 平台定时兜底** 自测 18/18 + 完整循环 13/13 + **生产端到端验证通过**（无凭证 401 / 正确 secret 200），`GET|POST /api/v1/sync/scheduled` + CRON_SECRET 恒定时间比较 + fail closed + `vercel.json` 两条 cron（10:00/22:00 UTC，顺带保活 Supabase）。**本卡真正的关键改动是同步层补强制 `userId` 过滤** —— service role 绕过 RLS，靠隐式隔离会跨用户串数据且不报错。~~migration 补档~~ **2026-09-05 核实推翻**：同步相关表与列全部在 `20260902003000_initial_schema.sql`，schema 一直版本化，无需补档。剩 ~~P0-2-7 状态 UI~~ ✅ / 2-8 过期提醒 / 2-9 撤销授权 / 2-11 总览合并。
>
> **✅ P0-2-7 同步状态 UI 验收通过（2026-09-05 15:35，Steven 浏览器验收，第 18 张卡）—— M2 推进到 8/11**：顶部状态条与卡片行的四态实测通过，3 个 `p027-*` 测试 auth 号已删干净，无遗留闭环。**下一张卡 P0-2-8**（Canvas token 过期提醒）已开工并验收通过（2026-09-05 晚）。
> **✅ P0-2-8 Canvas token 过期提醒 验收通过（2026-09-05 晚，Steven 浏览器验收，第 19 张卡）—— M2 推进到 9/11**：dashboard 顶部 warning / expired 横幅 + T3 过期自动翻 `expired` 并跳过同步，自测 31/31 全绿（纯函数 17 + dashboard SSR 10 + T3 过期跳过 4）。**下一张卡 P0-2-9**（撤销授权入口）。
> **✅ P0-2-9 撤销授权入口 验收通过（2026-09-05 21:30，Steven 浏览器验收，第 20 张卡）—— M2 推进到 10/11**：课程详情页新增账号级撤销入口 + 二次确认（Steven 拍板：入口放详情页 / 保留各课 `canvas_course_id` / 复用解除关联确认框）。🔴 **「删除加密凭证」= 占位密文覆盖而非置空** —— `secret_encrypted` NOT NULL 且同步先解密再判 status，置空会让同步整趟 500；端到端关键判据「撤销后打同步 → 401 而非 500」通过。**顺手修 P0-2-8 口子**：`revoked` 时过期提醒横幅不再出现（否则用户刚断开就被催着重连）。自测 44/44，commit `40afd47` + `31b47a4`，Vercel 部署 success。**下一张卡 P0-2-11 总览页合并（M2 最后一张）**。
> **✅ P0-2-11 总览页合并 验收通过（2026-09-05 深夜，第 21 张卡）—— M2 收官 11/11**：**本卡零功能代码** —— 合并机制早已随 P0-1-9 + P0-2-5 落地（总览页只读 `tasks` 一张表，两类数据各写各的 `source`），端到端核对 28/28 全绿、无缺陷：两类混排 + `dueDate` 升序 + TBD 排最后 / 逾期保留在 7 天窗口 / **课程页日期 == 总览页日期（ADR-004 验收标准）** / 考试行删除 → 派生 task 物理删除 / **二次 Canvas 同步后考试任务完全不变**（合并展示最高风险项，靠两边查询都带 `.eq('source', …)` 收口）/ `done` 不被打回 / 归档课程任务隐藏。交付是 2 处契约收口（`API-Contract.md` §5 的「只有 syllabus 考试」→ 两类来源对照表；`meta` 的 `staleWarning`/`lastSuccessfulSyncAt` 收口为**确定不加**，P0-2-7 已用 `SyncStatusBar` 服务端直读解决）。**Steven 拍板：不加 Canvas 作业来源标签**（标签的价值在于标出少数派）。**M2 全线收官，下一阶段 M3（P0-3 验证与收尾）。**
> ~~🔵 P0-2-11 总览页合并 核对完成（2026-09-05 晚，Bud，端到端 28/28，待 Steven 拍板一处 + 浏览器验收）~~：**领卡先判断出这张卡不是从零写功能** —— 总览页只读 `tasks` 一张表，两类数据早已各写各的行（考试派生 `source='syllabus'` / Canvas 同步 `source='canvas'`），生产截图里已混排。核对 7 组：两类混排 + `dueDate` 升序 + TBD 排最后 / 逾期保留在 7 天窗口 / **课程页日期 == 总览页日期（ADR-004 验收标准）** / 考试行删除 → 派生 task 物理删除 / **二次 Canvas 同步后考试任务完全不变**（合并展示最高风险项）/ `done` 不被保存打回 / 归档课程任务隐藏 —— **28/28 全绿，无功能缺陷**。交付是 2 处契约收口：`API-Contract.md` §5「只有 syllabus 考试」改为两类来源对照表；`meta` 的 `staleWarning`/`lastSuccessfulSyncAt` 收口为**确定不加**（P0-2-7 已用 dashboard 顶部 `SyncStatusBar` 服务端直读解决）。**待 Steven 拍板**：要不要给 Canvas 作业加「作业」来源标签（现状只有考试有标签）。
> **🔵 P0-2-9 撤销授权入口 代码完成（2026-09-05 晚，Bud，自测 44/44，待 Steven 浏览器验收）**：`DELETE /api/v1/canvas/credentials` —— 未登录 401 / 无凭据或已撤销 404（**幂等**，`revoked_at` 不刷新）/ 成功 200 且 `status='revoked'`；**已同步 `tasks` 与各门课 `canvas_course_id` 全部保留**（重连后立刻恢复同步，不必逐门课重新关联）。入口按 Steven 拍板放在课程详情页「Canvas 关联」区块，带二次确认（复用「解除关联」的确认框，两框互斥）。🔴 **「删除加密凭证」= 用占位密文覆盖**：`secret_encrypted` 是 `NOT NULL`，且 `runCanvasSync` 的顺序是**先 `decryptSecret()` 再判 `status !== 'active'`**，置空串会让解密抛错、同步从"优雅跳过"退化成"整趟 500"。🔴 **顺带收掉 P0-2-8 的一个口子**：`status='revoked'` 时 `toCredentialExpiryView` 也返回 null —— 否则撤销后 dashboard 仍显示「令牌将在 X 天后过期，记得去 bCourses 重新生成」，用户刚主动断开却被催着重连。自测 44/44（纯函数 14 + 端到端 30，含"撤销后打同步 → 401 而非 500"与 dashboard 横幅对照组）+ tsc/lint/build ✅。**下一张卡 P0-2-11**（总览合并；P0-2-10 判定不执行）。

> **🔵 P0-2-8 Canvas token 过期提醒 代码完成（2026-09-05，Bud，待 Steven 浏览器验收）**：基于用户实际 `expires_at` 的 T-14/T-7/T-3/T-1/当天 应用内横幅（per-session 可关闭，复用 P0-2-7 失败横幅的重连入口）；T3 定时扫描发现过期自动把 `status` 翻 `expired` 并跳过同步（§10「不浪费请求」）。新增 `lib/sync/expiry.ts` 纯函数 + `components/sync/token-expiry-banner.tsx`；dashboard 凭据查询移出 `hasCanvasLink` 守卫（有凭据未关联课也提示）。自测 31/31（纯函数 17 + dashboard SSR 10 + T3 过期跳过 4）+ tsc/lint/build ✅。**下一张卡 P0-2-9**（撤销授权）。
>
> **P0-2-7 同步状态 UI ✅ 验收通过（2026-09-05 15:35，Steven 浏览器验收，第 18 张卡）**：dashboard 顶部汇总状态条 + 每门课卡片一行，五态（not_linked / ok / stale / never / failed）；失败**分级** —— 凭证不可用给「去重新连接 Canvas」且不给重试按钮，凭证可用却失败给重试 + 说明自动重试。**Steven 拍板**：不轮询、不建 `GET /api/v1/sync/status`（服务端组件直读 Supabase 即可）；token 续期提醒剥离给 P0-2-8。🔴 **`last_synced_at` 语义修正**：此前失败也写该列，导致失败后 UI 把"失败那一刻"当成"最后同步时间"（旧数据伪装成新的），已改为**失败不推进该列**并用真实 PAT 端到端验证。自测：纯函数 45/45 + dashboard SSR 28/28 + 真实 Canvas 端到端 16/16。**下一张卡 P0-2-8（token 过期提醒）。**

**关键交付**
- 5-6 名混合构成的种子用户跑通完整旅程（含异常路径）
- 量化指标采集埋点就绪（编辑修正率、7 日回访、人均关联课程数）
- 用户修正数据以 diff 形式落库
- 隐私最小交代 + 一键删除可用

**主要风险**
- ~~Canvas token 入口被学校关闭 → iCal Feed 兜底~~ ✅ **已排除**：P0-2-1 实测入口可用（2026-09-01），iCal 保留为长期 Plan B（[ADR-002](./Decisions.md#adr-002)）
- 种子用户全是技术党 → 反馈偏差，结论不可信
- 冷启动链路过长导致流失 → Demo Workspace + 解析过程可视化

### Gate 0→1

进入 Phase 1 前，**必须**满足：

| # | 条件 | 类型 |
|---|---|---|
| G0-1 | 5-6 名种子用户（含至少 2 名非技术型）完整跑通主旅程 | 硬性 |
| G0-2 | 编辑修正率 ≥ 50% | 硬性 |
| G0-3 | 人均关联课程数 ≥ 2 | 硬性 |
| G0-4 | 明确的用户主观反馈：确实减少了打开 Canvas 的次数 | 硬性 |
| G0-5 | 至少 3 人表示"下学期还会继续用" | 硬性 |
| G0-6 | 同步失败场景全部有显式提示，无静默展示旧数据的情况 | 硬性 |
| G0-7 | 7 日回访次数 ≥ 3 次/人 | 观察项（不达标需复盘，不必然阻塞） |

**未达标的后果**：若 G0-2 / G0-3 不达标 → 先修解析质量或接入门槛，**不要**进入 Phase 1（用 OAuth 解决不了"解析不准"或"关联太麻烦"的问题）。若 G0-4 不达标 → 内核假设未验证，需重新审视产品方向。

---

## 5. Phase 1 — 正式动态感知层

**状态**：⚪ 未开始 ｜ **依赖**：Gate 0→1 通过

**核心命题**：能不能让**不想折腾 token 的普通学生**也无痛接入，并把数据源铺开。

**关键交付**
- Canvas OAuth2 正式集成（伯克利 Developer Key 机构审批）
- Google Calendar OAuth 集成（读/写日程）
- 自动定时轮询 + **增量同步与变更检测**
- Canvas Calendar Feed（iCal）兜底与交叉验证
- 课程自动匹配辅助（**保留人工确认**）
- 课程网页抓取兜底（明确告知用户不保证稳定）

**主要风险**
- **机构审批周期长**（最大风险）→ **缓解：Phase 0 初见成效时立即启动申请，不等 Phase 0 结束**。此时手里有真实用户与数据，谈判筹码完全不同
- 增量同步的变更检测做不好 → 直接卡住 Phase 2（规划依赖干净的输入）
- Scope 过大引发学校顾虑 → 严格限制在 read

**明确不做**：CalCentral 集成（[ADR-007](./Decisions.md#adr-007)）

### Gate 1→2

| # | 条件 |
|---|---|
| G1-1 | OAuth 一键授权流程可用，非技术用户能独立完成 |
| G1-2 | 增量同步 + 变更检测稳定（能正确识别 due date 变更、新作业发布） |
| G1-3 | 至少 2 个数据源同时工作且数据一致（无日期打架） |
| G1-4 | 同步延迟在用户可接受范围内（无"错过 deadline"反馈） |
| G1-5 | 用户规模较 Phase 0 有实质增长（不再限于愿折腾的同学） |

---

## 6. Phase 2 — 规划智能层（内核成型）

**状态**：⚪ 未开始 ｜ **依赖**：Gate 1→2 通过

**核心命题**：学生会不会**照着 Tempo 给的计划行动**，而不是看完再回 Canvas 确认一遍。

**关键交付**
- 每日任务自动生成（按 grade weight / 剩余时间 / 任务类型加权排序）
- 大项目任务分解（长期任务拆解到每天）
- 动态调整：未完成任务合理顺延（均摊而非堆积），支持手动拖拽
- 进度感知：基于同步到的**真实完成状态**而非初始计划
- 总览页可视化升级：动态进度条、时间线视图

**主要风险**
- 排序黑盒 → 用户不理解"为什么先做这个" → **可解释性是硬要求**
- 一刀切的分解逻辑显得不了解学生实际节奏 → 需支持个性化调整
- 顺延堆积造成"任务爆炸" → 必须给用户可控性

### Gate 2→3

| # | 条件 |
|---|---|
| G2-1 | 用户把 Tempo 每日任务列表当作"今天要做的事"的唯一入口 |
| G2-2 | 排序结果可解释，用户能理解优先级理由 |
| G2-3 | 顺延逻辑未引发"任务爆炸"的负面反馈 |
| G2-4 | 规划输入来自真实同步进度（而非静态计划） |

---

## 7. Phase 3 — 轻量代理层（谨慎推进）

**状态**：⚪ 未开始 ｜ **依赖**：Gate 2→3 通过

**核心命题**：在官方 API 支持范围内，敢不敢让 Tempo 替你动手。

**分级**
- **B1（低风险，优先做）**：有官方 API 支持的写操作，如自动创建 Google Calendar 事件
- **B2（高风险，opt-in 实验）**：无官方 API 的平台（Gradescope 自动提交、Piazza 自动回复）

**B2 的三条铁律**（总蓝图已定，此处重申）
1. 用户**主动开启**（opt-in），不进主流程
2. 必须有**清晰的失败可见性**（"已提交成功"要有实证展示）
3. **不作为核心卖点宣传**——核心卖点始终是 Phase 0-2 的实时检测与规划

### Gate 3→4

| # | 条件 |
|---|---|
| G3-1 | B1 类写操作成功率稳定，且失败可回滚 |
| G3-2 | 无任何"因为相信 Tempo 而误事"的事故 |
| G3-3 | 学校官方未对 Tempo 提出合规质疑 |

---

## 8. Phase 4 — 生态与资源层

**状态**：⚪ 未开始 ｜ **依赖**：Gate 3→4 通过

**核心命题**：跨用户数据沉淀能否形成网络效应，让 Tempo 从工具变成平台。

**关键交付**
- Past exam / past paper 资源库（UGC vs 官方合作两条路径）
- 课程/教授评价整合
- 跨学期数据沉淀（反哺 Phase 0 解析准确率与 Phase 2 规划智能）

**主要风险**
- **真正的法律风险**（UGC 版权 + FERPA 隐私），与 Canvas PAT 的 ToS 风险**性质完全不同**，启动前单独评估（[ADR-008](./Decisions.md#adr-008)）
- 网络效应起不来的冷启动问题

---

## 9. 学期节奏约束（重要）

Tempo 的验证强依赖**学期周期**——这是纯技术项目没有的硬约束，规划时必须考虑：

| 约束 | 影响 |
|---|---|
| **Fall 2026 学期正在进行中**（当前 2026-09） | Phase 0 必须在本学期内跑完，否则要等 Spring 2027 才有真实课程数据，验证窗口直接推后 4 个月 |
| **期中（约 10 月）是 syllabus 价值的峰值** | 考试日期、评分构成的关注度在学期初和期中最高，Phase 0 应赶在这个窗口前上线 |
| **期末（约 12 月）是"每日任务"价值的峰值** | Phase 2 的规划能力若在期末前上线，验证效果最好 |
| **学期初（1 月 / 8 月）是获客窗口** | 学生重新整理课程的动机最强，规模化动作应对齐学期初 |
| **寒暑假是数据真空期** | 不适合做验证，适合做工程重构与下一阶段准备 |

**推论**：
1. **Phase 0 的核心价值验证应优先于工程完善**——错过 Fall 2026 这个窗口，代价不是一个 sprint，是一个学期。
2. **Phase 1 的 OAuth 申请时机（Steven 拍板 2026-09-01：不提前，等验证结果）**
   - 原建议是"并行申请以节省时间"，Steven 决定**改为串行**：申请策略与说辞应基于 Phase 0 的真实用户数据，空手去谈的筹码远不如带着结论去谈。
   - ⚠️ **代价**：Phase 1 的 OAuth 大概率赶不上 Fall 2026 学期。
   - ✅ **约束**：Gate 0→1 评审通过后**必须立即启动申请**，不得继续拖延——到那时这一条的代价就从"晚一点"变成"再损失一个学期"。
3. **不要在期末做破坏性重构**——用户正在依赖 Tempo 备考。

---

## 10. 已放弃 / 明确不做（跨 Phase）

| 项目 | 决策 | 依据 |
|---|---|---|
| CalCentral 集成 | 不做 | [ADR-007](./Decisions.md#adr-007) |
| Gradescope 自动提交（进主流程） | 不做，仅 opt-in 实验 | 总蓝图 Phase 3 B2 类 |
| 移动端原生 App | 不做，响应式网页即可 | PRD Non-Goals |
| Canvas PAT 作为长期方案 | 否，验证期临时手段 | [ADR-002](./Decisions.md#adr-002) |

---

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-01 | 初版创建：Phase 0-4 看板、内核能力成熟度阶梯、各阶段 Gate、学期节奏约束 |
| 2026-09-01 | P0-2-1 实测结论同步：M2 风险「token 入口被关」排除并降级；OAuth 申请由「并行启动」改为「Phase 0 验证后立即启动」（Steven 拍板） |
| 2026-09-04 | **M2 从「未开工」改为「进行中（4/11）」**：P0-2-1 / 2-2 / 2-3 / 2-4 四张卡依次验收通过，凭据接入链路打通（加密存储 → 代理请求 → 课程列表 → 手动关联）。**下一张 P0-2-5 首次全量同步**（Canvas 作业 → `tasks` 落库，去重 + 更新） |
| 2026-09-04 | **P0-2-5 首次全量同步代码完成（🔵 待验收，自测 80/80）**：同步编排 + 落库 + `POST /api/v1/sync/now` + 关联后触发同步。Steven 拍板三件事：无 due date 的作业同步为 TBD、关联后立刻同步、跳过 `upcoming_events` 主扫描。真实数据：CHEM 1A 25 条（9 条 TBD）/ PHYSICS 7A 6 条 / MATH 53 LEC 0 条（空课程不是失败态），三门课串行 6.0 秒。**实现期修正一处设计**：节流改到「锁 → 凭据 → 课程」之后，否则 token 失效时用户会看到「同步太频繁」这种错误引导。M2 剩 P0-2-6 / 2-7 / 2-8 / 2-9 / 2-11 |
| 2026-09-04 | **P0-2-5 Steven 验收通过（✅ 13:44，「效果完美」），M2 推进到（5/11）**：Steven 在 Chem 1A 详情页「解除→重绑」触发同步，作业即落库并出现在总览卡片。**根因坐实**：Chem 1A 是在旧版（canvas-link 未接同步的 P0-2-4）关联的，重登/刷新从不触发同步（自动同步是 P0-2-6）→ tasks 一直空。**Math 53 LEC/DIS 均 0 条作业，「近期没有待办」是正确状态。** 下一张 **P0-2-6 刷新机制（下窗口继续）**：手动同步按钮（本次暴露：没有按钮只能靠「解除→重绑」触发）+ 打开应用自动同步 + 定时兜底 + ~~migration 补档~~（**2026-09-05 核实推翻**，见下一行） |
| 2026-09-05 | **P0-2-6 第一拍代码完成（🔵 自测 18/18）**：T1 打开应用/聚焦标签页自动同步（`app_open`，60s 节流）+ T2 手动「同步 Canvas」按钮（30s 节流），`/sync/now` 新增可选 `trigger` body（双档映射服务端权威，`scheduled`/非法值 400，裸 POST 回退 manual）。T3 定时兜底 = 第二拍，需 service role。**同日文档修正**：~~migration 补档~~ 伪命题核实推翻 —— `sync_runs` / `courses` 同步列（L55/L58-61）/ `canvas_credentials`（L198）/ `sync_runs`（L219）全部在 `20260902003000_initial_schema.sql`，schema 一直版本化；旧结论源于 09-04 诊断时无 service role 读不到生产 DB 的误判，已在 Phase-0-MVP / API-Contract / MEMORY.md 同源修正 |
| 2026-09-05 | **P0-2-6 第一拍 ✅ Steven 验收通过**（T1 自动同步 + T2 手动按钮）。**第二拍（T3 平台定时兜底）同日代码完成（🔵 待验收，自测 18/18）**：`GET|POST /api/v1/sync/scheduled`（CRON_SECRET 恒定时间比较 + 未配置即拒绝执行的 fail closed）+ `lib/sync/scheduled.ts`（串行逐用户、240s 总预算、响应不含用户数据）+ `lib/supabase/admin.ts`（service role 唯一入口）+ `vercel.json` 两条 cron（10:00/22:00 UTC）。**🔴 同步层四处查询补强制 `userId` 过滤**：service role 绕过 RLS，原先靠 RLS 隐式隔离的凭据 / 锁 / 节流 / 课程查询会跨用户串数据且**不报错**。⚠️ 真 service role 跑完整循环尚未实测，等 Steven 把 key 配进 `.env.local` |
| 2026-09-05 | **P0-2-6 两拍全部验收通过 ✅（Steven 生产验收，第 17 张卡），M2 推进到 7/11**：Steven 交付 service role key 后补跑盲区 —— **完整循环实测 13/13**（跨用户隔离探针通过：B 有凭据有课不关联 → 任务保持 0；A 清空后由定时扫描拉回 25 条；二跑幂等全 0；响应不含 UUID/课程名）；随后 Steven 配好 Vercel 两变量并 Redeploy，**生产端到端验证通过**（无凭证 `401 unauthorized` / 正确 secret `200`，`usersSynced=1` 幂等无丢数据）。8 个测试 auth 号已删除干净。**下一张 P0-2-7 同步状态 UI**（最后同步时间 + 失败可见性，依赖已满足、无外部依赖），执行卡已预置在 `Phase-0-MVP.md`，Steven 在新会话开工 |
| 2026-09-05 | **P0-2-7 同步状态 UI ✅ 验收通过（Steven 浏览器验收，第 18 张卡），M2 推进到 8/11**：顶部汇总状态条 + 课程卡片行的四态（ok / stale / never / failed）浏览器实测通过，3 个 `p027-*` 测试 auth 号已删除干净，卡已闭环无遗留。**本轮澄清一个用户侧认知问题（Steven 问「同步是每次登录自动还是必须手动」）**：自动同步的触发条件是**打开 dashboard 页面**（组件挂载 / 页面重新可见，60s 节流），不是"登录"动作本身 —— 只因登录后落地页就是 `/dashboard` 才体感等同。三个"以为没同步"的真实原因：未关联 Canvas 课时组件不渲染且一个请求都不发 / 自动同步成功是**静默**的（不弹提示，可见性靠本卡状态条）/ `SyncControls` 只挂在 dashboard（在课程详情页停留不会触发）。**T4 外部调度器（cron-job.org 每 60 分钟）确认从未配置**，判断暂不需要（T1 + T3 已覆盖），待 P0-2-8 后再定。**下一张 P0-2-8（Canvas token 过期提醒），Steven 在新会话开工** |
| 2026-09-05 | **P0-2-7 同步状态 UI 代码完成（🔵 自测 89/89，待 Steven 浏览器验收）**：顶部汇总状态条 + 每课卡片一行，五态；失败分级（能修 → 重新连接入口、不给重试；只能等 → 重试 + 说明自动重试）。Steven 拍板四项：放顶部+卡片、不轮询不建 `/sync/status`、续期提醒归 P0-2-8、文案分级。🔴 顺带修掉一个**语义级 bug**：`last_synced_at` 此前失败也写，导致失败后 UI 把失败时刻当"最后同步时间"（旧数据伪装成新的，违反 §9「禁止同步失败后继续展示旧数据且不提示」）—— 改为失败不推进该列，真实 PAT 端到端验证。自测：纯函数 45/45 + SSR 28/28 + 真实 Canvas 端到端 16/16。**M2 剩 P0-2-7 验收 → 2-8 / 2-9 / 2-11** |
| 2026-09-05 | **P0-2-9 撤销授权入口 代码完成（🔵 自测 44/44，待 Steven 浏览器验收），M2 交付到 10/11**：`DELETE /api/v1/canvas/credentials`（未登录 401 / 无凭据或已撤销 404 且幂等 / 成功 200 + `status='revoked'`），**已同步 `tasks` 与各门课 `canvas_course_id` 全部保留**（重连后立刻恢复同步）；入口按 Steven 拍板放课程详情页「Canvas 关联」区块 + 二次确认。🔴 两个关键实现点：① **「删除加密凭证」= 占位密文覆盖** —— `secret_encrypted` 是 `NOT NULL`，且 `runCanvasSync` 的顺序是**先 `decryptSecret()` 再判 `status !== 'active'`**，置空串会让解密抛错、同步从"优雅跳过"退化成"整趟 500"；② **收掉 P0-2-8 的一个口子** —— `status='revoked'` 时 `toCredentialExpiryView` 也返回 null，否则撤销后 dashboard 仍显示「令牌将在 X 天后过期，记得去 bCourses 重新生成」，用户刚主动断开却被催着重连。自测 44/44（纯函数 14 + 端到端 30，含"撤销后打同步 → 401 而非 500"、dashboard 横幅对照组、跨用户隔离）+ tsc/lint/build ✅，2 个 `p029-*` 测试 auth 号与临时脚本均已删。**M2 仅剩 P0-2-11 总览合并**（P0-2-10 按 P0-2-1 结论不执行） |
| 2026-09-05 | **P0-2-9 验收通过 ✅（Steven 浏览器验收，第 20 张卡）—— M2 已验收 10/11，剩 P0-2-11 总览页合并**：浏览器确认撤销入口位置、文案与显隐均正确。关键实现教训「撤销 = 占位密文覆盖而非置空」（置空会让同步从优雅跳过退化成整趟 500）已沉淀进 `CodingRules.md` §10.1 第 16 条 + §10.2 坑索引。commit `40afd47` + `31b47a4`，已 push，Vercel 部署 success |
| 2026-09-05 | **M2 动态感知正式收官（11/11 全部验收，已验收 21 张卡），进入 M3 验证与收尾**：里程碑表补 M3 行（P0-3-1 指标埋点 → 3-2 设置与隐私页 → 3-3 种子用户招募【Steven】→ 3-4 验证跑通 → 3-5 反馈整理 → 3-6 Gate 0→1 评审，风险「种子用户构成决定结论可信度」）。同期收口：`Phase-0-MVP.md` 进度指针去掉过时的「M2 剩余 P0-2-11」改为「下一阶段 M3」；`.workbuddy/memory/MEMORY.md` 超限压缩（11KB → 4.5KB，细节下放到 CodingRules §10 与各执行卡） |
| 2026-09-07 | **M3 开工：P0-3-1 量化指标埋点 代码完成（🔵 待 Steven 跑迁移 + 验收）** —— 里程碑表 M3 由 ⚪ 未开工改为 🔵 进行中。四项指标中**两项无需新埋点**（编辑修正率读 `parse_corrections`、人均关联课程数读 `courses.canvas_course_id`），新增 `usage_events` 只服务另外两项（7 日回访 / token 续期完成率）；读数入口 `GET /api/v1/metrics`（CRON_SECRET + service role，不建后台页面）。**⚠️ 验收前【Steven 手动】执行 `supabase/migrations/20260907120000_usage_events.sql`** —— 未执行时该端点返回 500（刻意设计：宁可报错也不返回假 0） |
| 2026-09-09 | **P0-3-1 验收通过 ✅ + P0-3-2 设置与隐私页 代码完成 🔵（待浏览器验收）**：P0-3-1 由 Steven 执行迁移后生产实测四项读数（冷启动态：两项 `null`、人均 1.0），他随后删掉两个稀释人均的测试号。P0-3-2 交付设置页（数据清单 + 隐私说明 + 三个危险操作）与三个端点，**自测在生产库走完整 HTTP 链路 23/23**（含 Storage 孤儿文件清理与「手动任务/syllabus 未被 `scope=canvas` 波及」两项关键对照）。🔵 **roadmap 调整暂缓（Steven 2026-09-07 拍板）**：他先写「发给种子用户前想改的东西」文档，定稿后再一起调 M3/M4 编号。**在此之前不动编号** | P0-3-1 / P0-3-2 |
| 2026-09-06 | **工具链与协作方式变更（非 task 卡，commit `f65cc65`，已 push）**：VS Code 从裸装配置为 Steven 的「阅读/学习台」（5 个扩展写进 `.vscode/extensions.json`），并新增 `CodingRules.md` **§6.1「交付必附『给 Steven 的三行』」** —— 每张卡交付时先给一段人话（点名 ≤2 个文件 + 一个反直觉的点 + **可点的**验收路径），让 Steven 从「盲签」变「签字」。起因是他提出「代码全是 Bud 写的、我看不懂，但想边做边学又不拖慢进度」。**不影响 M3 范围与排期**：编辑器扩展不参与构建、总结不碰代码，对质量与效率均为零影响；主动砍掉 CodeTour 导览（绑行号会漂，过期导览比没有更糟）与代码注释铺设（会腐烂）。**先试两张卡再决定是否延续**，全部可逆。**进度指针已推进至 `P0-3-1`（M3 第一张卡，依赖 P0-2-11 ✅ 已满足），Steven 在新会话开工**；M3 的 P0-3-3 种子用户招募 owner 是 Steven，需早打招呼 |
